---
phase: 05-polish-release
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/i18n/locales/en.json
  - src/i18n/locales/fr.json
  - src/i18n/locales/de.json
  - src/i18n/locales/it.json
autonomous: true
requirements:
  - QA-05

must_haves:
  truths:
    - "All 4 locale files have identical key sets (zero missing keys)"
    - "No locale file contains empty string values"
    - "No locale file contains English text in a non-EN locale (except proper nouns like OpenShift, RHACM, ODF)"
    - "Swiss-specific terminology used where appropriate (fr-CH, de-CH, it-CH)"
  artifacts:
    - path: "src/i18n/locales/en.json"
      provides: "English locale — reference"
    - path: "src/i18n/locales/fr.json"
      provides: "French (Swiss) locale"
    - path: "src/i18n/locales/de.json"
      provides: "German (Swiss) locale"
    - path: "src/i18n/locales/it.json"
      provides: "Italian (Swiss) locale"
  key_links:
    - from: "src/i18n/locales/*.json"
      to: "vue-i18n t() calls"
      via: "key lookup"
      pattern: "t\\("
---

<objective>
Full QA pass on all 4 i18n locale files: verify key completeness, grammar, technical accuracy, and Swiss locale correctness.

Purpose: Ensure every string renders correctly in all 4 languages with no missing keys, empty values, or translation errors.
Output: All 4 locale files verified and corrected if needed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/i18n/locales/en.json
@src/i18n/locales/fr.json
@src/i18n/locales/de.json
@src/i18n/locales/it.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated key completeness and empty-value audit</name>
  <files>src/i18n/locales/en.json, src/i18n/locales/fr.json, src/i18n/locales/de.json, src/i18n/locales/it.json</files>
  <read_first>
    - src/i18n/locales/en.json
    - src/i18n/locales/fr.json
    - src/i18n/locales/de.json
    - src/i18n/locales/it.json
  </read_first>
  <action>
Write and run a Node.js script that:
1. Loads all 4 locale JSON files
2. Flattens keys recursively (dot-notation)
3. For each non-EN locale, reports:
   - Keys present in EN but missing in the locale
   - Keys present in the locale but missing in EN (extras)
   - Keys with empty string values ("")
   - Keys whose value is identical to EN (potential untranslated strings) — EXCLUDE known proper nouns: OpenShift, RHACM, ODF, ROSA, ARO, OSD, SNO, HCP, MicroShift, BMC, Redfish, vCPU, MiB, GB, SSD, IOPS, vDU, CPU, RAM, CSV, PDF, PPTX, URL, HA, IoT, QPS
4. If any missing keys or empty values found: add the missing translations
5. For any identical-to-EN values that should be translated: translate them appropriately

For FR locale: use formal Swiss French (e.g., "septante" for 70 if applicable, standard formal register)
For DE locale: use Swiss German formal written standard (Hochdeutsch, no eszett — use "ss" instead of sharp-s)
For IT locale: use standard Italian as spoken in Ticino

Run the script and fix all issues found directly in the locale files.
  </action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/os-sizer && node -e "
const en=require('./src/i18n/locales/en.json');
const fr=require('./src/i18n/locales/fr.json');
const de=require('./src/i18n/locales/de.json');
const it=require('./src/i18n/locales/it.json');
function flat(o,p=''){return Object.entries(o).flatMap(([k,v])=>typeof v==='object'&&v!==null?flat(v,p+k+'.'):[[p+k,v]])}
const enE=flat(en);const enK=enE.map(e=>e[0]);
for(const [n,loc] of [['fr',fr],['de',de],['it',it]]){
  const locE=flat(loc);const locK=locE.map(e=>e[0]);
  const miss=enK.filter(k=>!locK.includes(k));
  const empty=locE.filter(([k,v])=>v==='').map(([k])=>k);
  if(miss.length)console.log(n+' MISSING:',miss);
  if(empty.length)console.log(n+' EMPTY:',empty);
  if(miss.length||empty.length)process.exit(1);
}
console.log('ALL LOCALES OK: identical key sets, no empty values');
"</automated>
  </verify>
  <acceptance_criteria>
    - The verification script prints "ALL LOCALES OK: identical key sets, no empty values"
    - The verification script exits 0
    - All 4 JSON files parse without error
  </acceptance_criteria>
  <done>All 4 locale files have identical key sets with zero missing keys and zero empty values</done>
</task>

<task type="auto">
  <name>Task 2: Grammar, technical accuracy, and consistency review</name>
  <files>src/i18n/locales/fr.json, src/i18n/locales/de.json, src/i18n/locales/it.json</files>
  <read_first>
    - src/i18n/locales/en.json
    - src/i18n/locales/fr.json
    - src/i18n/locales/de.json
    - src/i18n/locales/it.json
  </read_first>
  <action>
Review each non-EN locale file for:

1. **Grammar and spelling**: Read every value and fix any grammatical errors
2. **Technical accuracy**: Ensure OpenShift terminology is correctly translated:
   - "Control Plane Nodes" = FR: "Noeuds du plan de controle" / DE: "Control-Plane-Knoten" / IT: "Nodi del piano di controllo"
   - "Worker Nodes" = FR: "Noeuds de travail" / DE: "Worker-Knoten" / IT: "Nodi worker"
   - "High Availability" = FR: "Haute disponibilite" / DE: "Hochverfuegbarkeit" / IT: "Alta disponibilita"
   - Technology names (OpenShift, RHACM, ODF, etc.) stay in English
3. **Consistency**: Same term should use the same translation throughout the file
4. **Swiss specifics**:
   - DE: No eszett character — replace all instances of sharp-s with "ss" (e.g., "Groesse" not "Grosse")
   - FR: Standard formal register appropriate for business/technical documentation
   - IT: Standard formal Italian

Fix any issues found directly in the locale files. After fixes, run `npm run build` to ensure valid JSON.
  </action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/os-sizer && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - npm run build exits 0 (all JSON valid, app compiles)
    - grep -c 'ß' src/i18n/locales/de.json returns "0" (no eszett in Swiss German)
    - All 4 locale files are valid JSON (node -e "require('./src/i18n/locales/fr.json')" exits 0, same for de and it)
  </acceptance_criteria>
  <done>All translations are grammatically correct, technically accurate, consistent, and use Swiss-appropriate variants</done>
</task>

</tasks>

<verification>
- Zero missing keys across all 4 locales
- Zero empty string values
- No eszett in de.json
- `npm run build` passes
- All JSON files parse without error
</verification>

<success_criteria>
- All 4 locale files have identical 116+ key sets
- No empty values in any locale
- Swiss German uses "ss" not eszett
- Technical OpenShift terms correctly translated
- npm run build passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-release/05-02-SUMMARY.md`
</output>

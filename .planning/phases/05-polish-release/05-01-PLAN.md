---
phase: 05-polish-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.vue
  - src/components/shared/WizardStepper.vue
  - src/components/results/ResultsPage.vue
  - src/components/results/BomTable.vue
  - src/components/results/ExportToolbar.vue
  - src/components/wizard/Step1EnvironmentForm.vue
  - src/components/wizard/Step2WorkloadForm.vue
  - src/components/wizard/Step3ArchitectureForm.vue
  - src/style.css
autonomous: true
requirements:
  - QA-05

must_haves:
  truths:
    - "App is usable at 375px viewport width (no horizontal scroll, no overlapping elements)"
    - "All interactive elements are keyboard-navigable (Tab/Enter/Space)"
    - "All form inputs have associated labels (for attribute or aria-label)"
    - "Color contrast meets WCAG AA (4.5:1 for text)"
  artifacts:
    - path: "src/App.vue"
      provides: "Responsive header layout"
      contains: "sm:"
    - path: "src/style.css"
      provides: "Tailwind v4 CSS config with responsive utilities"
      contains: "@import"
    - path: "src/components/results/BomTable.vue"
      provides: "Responsive table with horizontal scroll wrapper"
      contains: "overflow-x-auto"
  key_links:
    - from: "src/App.vue"
      to: "all wizard step components"
      via: "responsive container classes"
      pattern: "max-w-4xl|px-4"
---

<objective>
Make the os-sizer app fully responsive for mobile (375px minimum) and accessible (keyboard navigation, ARIA labels, color contrast).

Purpose: Production readiness requires usability on phones/tablets and basic accessibility compliance.
Output: All Vue components updated with responsive Tailwind classes and ARIA attributes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/App.vue
@src/style.css
@src/components/shared/WizardStepper.vue
@src/components/results/ResultsPage.vue
@src/components/results/BomTable.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Responsive layout for App shell, header, and wizard stepper</name>
  <files>src/App.vue, src/components/shared/WizardStepper.vue, src/style.css</files>
  <read_first>
    - src/App.vue
    - src/components/shared/WizardStepper.vue
    - src/style.css
  </read_first>
  <action>
Update src/App.vue:
- Change header: replace `px-6 py-3` with `px-3 py-2 sm:px-6 sm:py-3`
- Change h1: replace `text-lg` with `text-base sm:text-lg`
- Change main: replace `max-w-4xl mx-auto p-6` with `max-w-4xl mx-auto px-3 py-4 sm:px-6 sm:py-6`
- Add `role="main"` to the main element
- Add `role="banner"` to the header element
- Add `aria-label` attribute with value "Application navigation" to the header nav area

Update src/components/shared/WizardStepper.vue:

- Make step labels hide on small screens: add `hidden sm:inline` class to the step label text spans
- Keep step numbers visible on all screens
- Ensure the stepper flex container wraps properly: use `flex-wrap` or responsive gap
- Add `role="navigation"` and `aria-label="Wizard steps"` to the stepper container
- Add `aria-current="step"` attribute on the active step element

Do NOT create tailwind.config.js. Do NOT use Tailwind v3 JIT syntax. All responsive utilities use Tailwind v4 native `sm:` / `md:` / `lg:` prefixes in class attributes.
  </action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/os-sizer && npm run type-check && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'px-3 py-2 sm:px-6' src/App.vue returns 0
    - grep -q 'role="main"' src/App.vue returns 0
    - grep -q 'role="banner"' src/App.vue returns 0
    - grep -q 'hidden sm:inline' src/components/shared/WizardStepper.vue returns 0
    - grep -q 'aria-current="step"' src/components/shared/WizardStepper.vue returns 0
    - grep -q 'role="navigation"' src/components/shared/WizardStepper.vue returns 0
    - No tailwind.config.js file exists
  </acceptance_criteria>
  <done>App shell and stepper render correctly at 375px with no horizontal overflow; ARIA roles present on header, main, and stepper</done>
</task>

<task type="auto">
  <name>Task 2: Responsive tables, forms, results, and ARIA labels on inputs</name>
  <files>src/components/results/BomTable.vue, src/components/results/ResultsPage.vue, src/components/results/ExportToolbar.vue, src/components/wizard/Step1EnvironmentForm.vue, src/components/wizard/Step2WorkloadForm.vue, src/components/wizard/Step3ArchitectureForm.vue</files>
  <read_first>
    - src/components/results/BomTable.vue
    - src/components/results/ResultsPage.vue
    - src/components/results/ExportToolbar.vue
    - src/components/results/TotalsSummaryCard.vue
    - src/components/wizard/Step1EnvironmentForm.vue
    - src/components/wizard/Step2WorkloadForm.vue
    - src/components/wizard/Step3ArchitectureForm.vue
    - src/components/shared/NumberSliderInput.vue
  </read_first>
  <action>
BomTable.vue:
- Wrap the table element in a div with `class="overflow-x-auto -mx-3 px-3 sm:mx-0 sm:px-0"` so it scrolls horizontally on mobile
- Add `role="table"` if not using native table element; if using native `<table>`, add `aria-label` with value from `t('results.title')`

ResultsPage.vue:

- Ensure the grid/flex layout for cards stacks vertically on mobile: use `grid grid-cols-1 sm:grid-cols-2` for TotalsSummaryCard and ArchOverviewCard area
- ExportToolbar buttons: ensure they wrap on small screens with `flex-wrap gap-2`

ExportToolbar.vue:

- Make buttons use `text-sm px-2 py-1 sm:px-3 sm:py-2` for mobile-friendly tap targets (minimum 44px height)
- Add `aria-label` to each export button using the i18n key text (e.g., `aria-label` bound to `t('results.toolbar.exportCsv')`)

Wizard form components (Step1, Step2, Step3):

- Audit all `<input>`, `<select>`, `<button>` elements
- Ensure every input has either a `<label for="...">` with matching `id`, or an `aria-label` attribute
- Ensure every select has `aria-label` if no visible label
- Add `aria-required="true"` to required form fields
- Radio button groups: add `role="radiogroup"` and `aria-label` to the container div

Do NOT create tailwind.config.js. Use only Tailwind v4 CSS-based config.
  </action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/os-sizer && npm run type-check && npm run build && npm run test</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'overflow-x-auto' src/components/results/BomTable.vue returns 0
    - grep -q 'aria-label' src/components/results/BomTable.vue returns 0
    - grep -q 'flex-wrap' src/components/results/ExportToolbar.vue returns 0
    - grep -q 'aria-label' src/components/results/ExportToolbar.vue returns 0
    - grep -q 'aria-label\|aria-required' src/components/wizard/Step1EnvironmentForm.vue returns 0
    - grep -q 'aria-label\|aria-required' src/components/wizard/Step2WorkloadForm.vue returns 0
    - grep -q 'aria-label\|aria-required' src/components/wizard/Step3ArchitectureForm.vue returns 0
    - npm run build exits 0
    - npm run test exits 0
  </acceptance_criteria>
  <done>All tables scroll on mobile, export buttons wrap, all form inputs have associated labels or aria-label, all required fields marked aria-required</done>
</task>

</tasks>

<verification>
- `npm run type-check` passes with zero errors
- `npm run build` produces clean production bundle
- `npm run test` all existing tests pass
- Manual spot-check: resize browser to 375px width, confirm no horizontal scroll on any page
</verification>

<success_criteria>

- App renders without horizontal overflow at 375px viewport
- All form inputs have aria-label or associated label elements
- Stepper navigation is keyboard-accessible
- ARIA roles on header, main, navigation
- No tailwind.config.js created (Tailwind v4 CSS-only config)
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-release/05-01-SUMMARY.md`
</output>

---
phase: 05-polish-release
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - QA-05

must_haves:
  truths:
    - "CI runs lint, type-check, and test on every pull request"
    - "CI runs build check on push to main"
    - "CI workflow uses Node 22 and npm ci"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "GitHub Actions CI pipeline"
      contains: "npm run lint"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "package.json scripts"
      via: "npm run commands"
      pattern: "npm run (lint|type-check|test|build)"
---

<objective>
Set up GitHub Actions CI pipeline: lint + type-check + test on PR, build check on main push.

Purpose: Catch regressions before merge, ensure production build stays clean.
Output: .github/workflows/ci.yml matching vcf-sizer pattern but with correct branch name.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GitHub Actions CI workflow</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - package.json (to confirm script names: lint, type-check, test, build)
  </read_first>
  <action>
Create the directory .github/workflows/ if it does not exist.

Create .github/workflows/ci.yml with a workflow named "CI" that:
- Triggers on pull_request to branches [main] and push to branches [main]
- Has one job called "check" running on ubuntu-latest
- Steps: checkout@v6, setup-node@v6 with node-version 22 and cache npm, npm ci
- Then 4 named steps: "Lint" (npm run lint), "Type-check" (npm run type-check), "Test" (npm run test), "Build" (npm run build)

Use branch name "main" (NOT "maincd" which vcf-sizer uses).
The Build step is added beyond the vcf-sizer pattern to ensure production builds stay clean.
  </action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/os-sizer &amp;&amp; test -f .github/workflows/ci.yml &amp;&amp; grep -q "npm run lint" .github/workflows/ci.yml &amp;&amp; grep -q "npm run type-check" .github/workflows/ci.yml &amp;&amp; grep -q "npm run test" .github/workflows/ci.yml &amp;&amp; grep -q "npm run build" .github/workflows/ci.yml &amp;&amp; echo "CI WORKFLOW OK"</automated>
  </verify>
  <acceptance_criteria>
    - File .github/workflows/ci.yml exists
    - grep -q 'npm run lint' .github/workflows/ci.yml returns 0
    - grep -q 'npm run type-check' .github/workflows/ci.yml returns 0
    - grep -q 'npm run test' .github/workflows/ci.yml returns 0
    - grep -q 'npm run build' .github/workflows/ci.yml returns 0
    - grep -q 'branches: \[main\]' .github/workflows/ci.yml returns 0
    - grep -q 'node-version' .github/workflows/ci.yml returns 0
    - The file does NOT contain 'maincd'
  </acceptance_criteria>
  <done>CI workflow runs lint, type-check, test, and build on every PR and push to main</done>
</task>

</tasks>

<verification>
- .github/workflows/ci.yml exists with all 4 npm run steps
- Targets main branch (not maincd)
- Uses Node 22 with npm cache
</verification>

<success_criteria>
- CI pipeline file created and valid YAML
- All 4 check steps present (lint, type-check, test, build)
- Branch targeting is correct (main)
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-release/05-03-SUMMARY.md`
</output>

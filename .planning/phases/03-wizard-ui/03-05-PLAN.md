---
phase: 03-wizard-ui
plan: 05
type: execute
wave: 4
depends_on: ["03-01", "03-02", "03-03", "03-04"]
files_modified:
  - src/i18n/locales/en.json
  - src/i18n/locales/fr.json
  - src/i18n/locales/de.json
  - src/i18n/locales/it.json
autonomous: true
requirements: [I18N-01, I18N-02, I18N-03, I18N-04, I18N-05, QA-04]

must_haves:
  truths:
    - "All 4 locale files contain recommendation.* keys matching recommendation.ts justificationKey values"
    - "All 4 locale files contain validation.* keys matching validation.ts messageKey values"
    - "All 4 locale files contain warning.* keys used by recommendation.ts warningKeys"
    - "All 4 locale files contain workload.nodeVcpu and workload.nodeRam"
    - "All 4 locale files contain wizard.step3.selectionRequired, wizard.step3.showOverride, wizard.step3.hideOverride"
    - "All 4 locale files contain warnings.tna.techPreview, warnings.tnf.techPreview, warnings.tnf.redfishRequired, warnings.managedCloud.noHardware"
    - "No missing i18n key warnings in the browser console when using any locale"
  artifacts:
    - path: "src/i18n/locales/en.json"
      provides: "Complete EN strings for all wizard phases"
    - path: "src/i18n/locales/fr.json"
      provides: "Complete FR strings for all wizard phases"
    - path: "src/i18n/locales/de.json"
      provides: "Complete DE strings for all wizard phases"
    - path: "src/i18n/locales/it.json"
      provides: "Complete IT strings for all wizard phases"
  key_links:
    - from: "src/i18n/locales/*.json"
      to: "src/engine/recommendation.ts"
      via: "justificationKey values must match keys in locales"
      pattern: "recommendation\\..*\\..*"
    - from: "src/i18n/locales/*.json"
      to: "src/engine/validation.ts"
      via: "messageKey values must match keys in locales"
      pattern: "validation\\..*"
---

<objective>
Complete i18n string coverage for all 4 locales (EN, FR, IT, DE). Add every key referenced by the wizard components, recommendation engine, and validation module that does not yet exist in the locale files.

Purpose: The wizard built in 03-01 through 03-04 will show raw key strings (e.g. "recommendation.standardHa.production") instead of translated text if locale files are incomplete. This plan closes that gap.

Output: All 4 locale JSON files fully populated; `npm run test` still passes; no missing keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@src/i18n/locales/en.json
@src/i18n/locales/fr.json
@src/i18n/locales/de.json
@src/i18n/locales/it.json
@src/engine/recommendation.ts
@src/engine/validation.ts
@.planning/phases/03-wizard-ui/03-04-SUMMARY.md

<interfaces>
<!-- All justificationKey and warningKey values from src/engine/recommendation.ts. -->
<!-- These are the keys that MUST exist in every locale file. -->

justificationKey values (from recommendation.ts):

- recommendation.standardHa.production
- recommendation.compact3Node.budgetFriendly
- recommendation.sno.edgeSingleSite
- recommendation.tna.minimalHa
- recommendation.tnf.twoNodeFencing
- recommendation.hcp.multiClusterEfficiency
- recommendation.microshift.constrainedEdge
- recommendation.managedCloud.noHardwareManagement

warningKeys values (from recommendation.ts):

- validation.techPreviewNotForProduction
- validation.redfishBmcRequired
- validation.managedCloudNoHardware

messageKey values from src/engine/validation.ts:

- validation.negativePods
- validation.workerRamBelowMin
- validation.workerCpuBelowMin
- validation.zeroPodsWithResources
- validation.odfIncompatibleTopology
- validation.rhacmNoClusters

Keys already present in all locales (DO NOT duplicate):

- wizard.step1.*, wizard.step2.*, wizard.step3.*, wizard.step4.*
- wizard.nav.previous, wizard.nav.next, wizard.nav.calculate
- environment.*, workload.*, topology.*, sno.*, hcp.*
- validation.required, validation.minPods, validation.minCpu, validation.minRam
- results.*, print.*

Keys referenced in component templates that may be MISSING:

- workload.nodeVcpu  (used in Step2WorkloadForm — check if exists)
- workload.nodeRam   (used in Step2WorkloadForm — check if exists)
- wizard.step3.selectionRequired  (used in WizardStepper)
- wizard.step3.showOverride       (used in Step3ArchitectureForm)
- wizard.step3.hideOverride       (used in Step3ArchitectureForm)
- warnings.tna.techPreview        (used in Step3ArchitectureForm)
- warnings.tnf.techPreview        (used in Step3ArchitectureForm)
- warnings.tnf.redfishRequired    (used in Step3ArchitectureForm)
- warnings.managedCloud.noHardware (used in Step3ArchitectureForm)
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete EN locale with all missing keys</name>
  <files>src/i18n/locales/en.json</files>
  <action>
  Read the current en.json. Identify which keys from the interface block above are missing. Add ONLY the missing keys — do not modify existing keys.

  Add the following sections (or merge into existing sections as appropriate):

  **workload additions** (if not present):

  ```json
  "nodeVcpu": "Worker Node vCPU",
  "nodeRam": "Worker Node RAM (GB)"
  ```

  **wizard.step3 additions** (merge into existing wizard.step3 object):

  ```json
  "selectionRequired": "Please select an architecture to continue",
  "showOverride": "Override recommendation",
  "hideOverride": "Hide override"
  ```

  **warnings section** (new top-level key, add before "print"):

  ```json
  "warnings": {
    "tna": {
      "techPreview": "Two-Node with Arbiter (TNA) is a Technology Preview feature — not supported for production workloads"
    },
    "tnf": {
      "techPreview": "Two-Node with Fencing (TNF) is a Technology Preview feature — not supported for production workloads",
      "redfishRequired": "Requires Redfish-compatible BMC management on both nodes"
    },
    "managedCloud": {
      "noHardware": "Managed cloud topologies (ROSA/ARO) are sized by the cloud provider — no hardware BoM is generated"
    }
  }
  ```

  **recommendation section** (new top-level key):

  ```json
  "recommendation": {
    "standardHa": {
      "production": "Best fit for production datacenter workloads requiring full high availability with N+1 failure tolerance"
    },
    "compact3Node": {
      "budgetFriendly": "Budget-friendly datacenter HA — control plane nodes double as workers, suitable for smaller workloads"
    },
    "sno": {
      "edgeSingleSite": "Single-node deployment optimised for edge and far-edge sites with constrained hardware budgets"
    },
    "tna": {
      "minimalHa": "Minimal HA configuration using an arbiter witness node — reduces hardware cost while maintaining quorum"
    },
    "tnf": {
      "twoNodeFencing": "Two-node bare-metal cluster with fencing — provides HA without an arbiter node via Redfish BMC"
    },
    "hcp": {
      "multiClusterEfficiency": "Hosted Control Planes decouple the control plane from worker nodes — efficient for large multi-cluster deployments"
    },
    "microshift": {
      "constrainedEdge": "Minimal footprint OpenShift for far-edge IoT and device workloads — runs on 2 vCPU / 2 GB"
    },
    "managedCloud": {
      "noHardwareManagement": "ROSA, ARO, or OSD — fully managed OpenShift with no hardware provisioning required"
    }
  }
  ```

  **validation additions** (merge into existing validation object):

  ```json
  "negativePods": "Pod count cannot be negative",
  "workerRamBelowMin": "Worker node RAM is below the 8 GB minimum — sizing may be invalid",
  "workerCpuBelowMin": "Worker node vCPU is below the 2 vCPU minimum — sizing may be invalid",
  "zeroPodsWithResources": "Pod count is zero but CPU/RAM requests are set — check your workload profile",
  "odfIncompatibleTopology": "ODF storage is not compatible with SNO, MicroShift, or Managed Cloud topologies",
  "rhacmNoClusters": "RHACM is enabled but no managed cluster count is set — enter expected cluster count",
  "techPreviewNotForProduction": "Technology Preview — not supported for production use",
  "redfishBmcRequired": "Requires Redfish-compatible BMC on all cluster nodes",
  "managedCloudNoHardware": "Managed cloud topology — no hardware BoM is generated"
  ```

  Write the complete updated en.json with ALL existing keys preserved plus the additions.
  </action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/os-sizer && node -e "JSON.parse(require('fs').readFileSync('src/i18n/locales/en.json','utf8')); console.log('valid JSON')"</automated>
  </verify>
  <done>

- en.json is valid JSON
- Contains recommendation.*, validation.negativePods and all other validation additions
- Contains warnings.tna.*, warnings.tnf.*, warnings.managedCloud.*
- Contains workload.nodeVcpu, workload.nodeRam
- Contains wizard.step3.selectionRequired, showOverride, hideOverride
- All existing keys preserved (no regressions)
  </done>

</task>

<task type="auto">
  <name>Task 2: Complete FR, DE, IT locales with translated strings</name>
  <files>
    src/i18n/locales/fr.json,
    src/i18n/locales/de.json,
    src/i18n/locales/it.json
  </files>
  <action>
  Apply the same additions to fr.json, de.json, it.json — using proper translations for each language. Preserve all existing keys in each file.

  **French (fr.json) additions:**

  workload additions:

  ```json
  "nodeVcpu": "vCPU du noeud worker",
  "nodeRam": "RAM du noeud worker (Go)"
  ```

  wizard.step3 additions:

  ```json
  "selectionRequired": "Veuillez selectionner une architecture pour continuer",
  "showOverride": "Remplacer la recommandation",
  "hideOverride": "Masquer le remplacement"
  ```

  warnings section:

  ```json
  "warnings": {
    "tna": {
      "techPreview": "Two-Node avec arbitre (TNA) est une fonctionnalite en apercu technologique — non prise en charge pour les charges de travail en production"
    },
    "tnf": {
      "techPreview": "Two-Node avec fencing (TNF) est une fonctionnalite en apercu technologique — non prise en charge pour les charges de travail en production",
      "redfishRequired": "Necessite un BMC compatible Redfish sur les deux noeuds"
    },
    "managedCloud": {
      "noHardware": "Les topologies cloud gerees (ROSA/ARO) sont dimensionnees par le fournisseur cloud — aucune nomenclature materielle n'est generee"
    }
  }
  ```

  recommendation section:

  ```json
  "recommendation": {
    "standardHa": {
      "production": "Meilleur choix pour les charges de travail en production en centre de donnees necessitant une haute disponibilite complete avec tolerance aux pannes N+1"
    },
    "compact3Node": {
      "budgetFriendly": "HA economique en centre de donnees — les noeuds du plan de controle font aussi office de workers, adapte aux charges de travail plus petites"
    },
    "sno": {
      "edgeSingleSite": "Deploiement a noeud unique optimise pour les sites edge et far-edge avec budget materiel limite"
    },
    "tna": {
      "minimalHa": "Configuration HA minimale avec un noeud temoin arbitre — reduit le cout materiel tout en maintenant le quorum"
    },
    "tnf": {
      "twoNodeFencing": "Cluster bare-metal a deux noeuds avec fencing — fournit la HA sans noeud arbitre via BMC Redfish"
    },
    "hcp": {
      "multiClusterEfficiency": "Les plans de controle heberges decoupl le plan de controle des noeuds workers — efficace pour les deploiements multi-cluster a grande echelle"
    },
    "microshift": {
      "constrainedEdge": "OpenShift a empreinte minimale pour les charges IoT et appareils far-edge — fonctionne sur 2 vCPU / 2 Go"
    },
    "managedCloud": {
      "noHardwareManagement": "ROSA, ARO ou OSD — OpenShift entierement gere sans provisionnement materiel requis"
    }
  }
  ```

  validation additions:

  ```json
  "negativePods": "Le nombre de pods ne peut pas etre negatif",
  "workerRamBelowMin": "La RAM du noeud worker est inferieure au minimum de 8 Go — le dimensionnement peut etre invalide",
  "workerCpuBelowMin": "Le vCPU du noeud worker est inferieur au minimum de 2 vCPU — le dimensionnement peut etre invalide",
  "zeroPodsWithResources": "Le nombre de pods est zero mais les requetes CPU/RAM sont definies — verifiez votre profil de charge",
  "odfIncompatibleTopology": "Le stockage ODF n'est pas compatible avec les topologies SNO, MicroShift ou Cloud gere",
  "rhacmNoClusters": "RHACM est active mais aucun nombre de clusters geres n'est defini — saisissez le nombre de clusters prevu",
  "techPreviewNotForProduction": "Apercu technologique — non pris en charge pour un usage en production",
  "redfishBmcRequired": "Necessite un BMC compatible Redfish sur tous les noeuds du cluster",
  "managedCloudNoHardware": "Topologie cloud geree — aucune nomenclature materielle n'est generee"
  ```

  ---

  **German (de.json) additions:**

  workload additions:

  ```json
  "nodeVcpu": "Worker-Knoten vCPU",
  "nodeRam": "Worker-Knoten RAM (GB)"
  ```

  wizard.step3 additions:

  ```json
  "selectionRequired": "Bitte waehlen Sie eine Architektur aus, um fortzufahren",
  "showOverride": "Empfehlung ueberschreiben",
  "hideOverride": "Ueberschreibung ausblenden"
  ```

  warnings section:

  ```json
  "warnings": {
    "tna": {
      "techPreview": "Two-Node mit Schiedsrichter (TNA) ist eine Technologievorschau-Funktion — nicht fuer Produktionsworkloads unterstuetzt"
    },
    "tnf": {
      "techPreview": "Two-Node mit Fencing (TNF) ist eine Technologievorschau-Funktion — nicht fuer Produktionsworkloads unterstuetzt",
      "redfishRequired": "Erfordert Redfish-kompatibles BMC-Management auf beiden Knoten"
    },
    "managedCloud": {
      "noHardware": "Verwaltete Cloud-Topologien (ROSA/ARO) werden vom Cloud-Anbieter dimensioniert — keine Hardware-Stueckliste wird erstellt"
    }
  }
  ```

  recommendation section:

  ```json
  "recommendation": {
    "standardHa": {
      "production": "Beste Wahl fuer Produktions-Rechenzentrum-Workloads mit vollstaendiger Hochverfuegbarkeit und N+1-Fehlertoleranz"
    },
    "compact3Node": {
      "budgetFriendly": "Kostenguenstige Rechenzentrum-HA — Control-Plane-Knoten fungieren auch als Worker, geeignet fuer kleinere Workloads"
    },
    "sno": {
      "edgeSingleSite": "Einzelknoten-Deployment optimiert fuer Edge- und Far-Edge-Standorte mit begrenztem Hardware-Budget"
    },
    "tna": {
      "minimalHa": "Minimale HA-Konfiguration mit einem Schiedsrichter-Zeugenknoten — reduziert Hardwarekosten bei Quorum-Erhalt"
    },
    "tnf": {
      "twoNodeFencing": "Zwei-Knoten-Bare-Metal-Cluster mit Fencing — bietet HA ohne Schiedsrichterknoten ueber Redfish-BMC"
    },
    "hcp": {
      "multiClusterEfficiency": "Gehostete Control Planes entkoppeln die Control Plane von Worker-Knoten — effizient fuer grosse Multi-Cluster-Deployments"
    },
    "microshift": {
      "constrainedEdge": "OpenShift mit minimalem Footprint fuer Far-Edge-IoT- und Geraete-Workloads — laeuft auf 2 vCPU / 2 GB"
    },
    "managedCloud": {
      "noHardwareManagement": "ROSA, ARO oder OSD — vollstaendig verwaltetes OpenShift ohne Hardware-Provisionierung"
    }
  }
  ```

  validation additions:

  ```json
  "negativePods": "Die Pod-Anzahl darf nicht negativ sein",
  "workerRamBelowMin": "Worker-Knoten-RAM liegt unter dem Minimum von 8 GB — die Dimensionierung koennte ungueltig sein",
  "workerCpuBelowMin": "Worker-Knoten-vCPU liegt unter dem Minimum von 2 vCPU — die Dimensionierung koennte ungueltig sein",
  "zeroPodsWithResources": "Pod-Anzahl ist null, aber CPU/RAM-Anforderungen sind gesetzt — pruefen Sie Ihr Arbeitslastprofil",
  "odfIncompatibleTopology": "ODF-Speicher ist nicht kompatibel mit SNO-, MicroShift- oder verwalteten Cloud-Topologien",
  "rhacmNoClusters": "RHACM ist aktiviert, aber keine Anzahl verwalteter Cluster ist angegeben — tragen Sie die erwartete Cluster-Anzahl ein",
  "techPreviewNotForProduction": "Technologievorschau — nicht fuer den Produktionseinsatz unterstuetzt",
  "redfishBmcRequired": "Erfordert Redfish-kompatibles BMC auf allen Cluster-Knoten",
  "managedCloudNoHardware": "Verwaltete Cloud-Topologie — keine Hardware-Stueckliste wird erstellt"
  ```

  ---

  **Italian (it.json) additions:**

  workload additions:

  ```json
  "nodeVcpu": "vCPU nodo worker",
  "nodeRam": "RAM nodo worker (GB)"
  ```

  wizard.step3 additions:

  ```json
  "selectionRequired": "Selezionare un'architettura per continuare",
  "showOverride": "Sostituire la raccomandazione",
  "hideOverride": "Nascondi sostituzione"
  ```

  warnings section:

  ```json
  "warnings": {
    "tna": {
      "techPreview": "Two-Node con arbitro (TNA) e una funzionalita di anteprima tecnologica — non supportata per i carichi di lavoro di produzione"
    },
    "tnf": {
      "techPreview": "Two-Node con fencing (TNF) e una funzionalita di anteprima tecnologica — non supportata per i carichi di lavoro di produzione",
      "redfishRequired": "Richiede la gestione BMC compatibile con Redfish su entrambi i nodi"
    },
    "managedCloud": {
      "noHardware": "Le topologie cloud gestite (ROSA/ARO) sono dimensionate dal provider cloud — nessuna distinta materiali hardware viene generata"
    }
  }
  ```

  recommendation section:

  ```json
  "recommendation": {
    "standardHa": {
      "production": "Scelta ottimale per i carichi di lavoro di produzione in datacenter che richiedono alta disponibilita completa con tolleranza agli errori N+1"
    },
    "compact3Node": {
      "budgetFriendly": "HA in datacenter economica — i nodi del piano di controllo fungono anche da worker, adatta a carichi di lavoro piu piccoli"
    },
    "sno": {
      "edgeSingleSite": "Distribuzione a nodo singolo ottimizzata per siti edge e far-edge con budget hardware limitato"
    },
    "tna": {
      "minimalHa": "Configurazione HA minima con un nodo testimone arbitro — riduce i costi hardware mantenendo il quorum"
    },
    "tnf": {
      "twoNodeFencing": "Cluster bare-metal a due nodi con fencing — fornisce HA senza nodo arbitro tramite Redfish BMC"
    },
    "hcp": {
      "multiClusterEfficiency": "I piani di controllo ospitati disaccoppiano il piano di controllo dai nodi worker — efficiente per distribuzioni multi-cluster di grandi dimensioni"
    },
    "microshift": {
      "constrainedEdge": "OpenShift a ingombro minimo per carichi di lavoro IoT e dispositivi far-edge — funziona su 2 vCPU / 2 GB"
    },
    "managedCloud": {
      "noHardwareManagement": "ROSA, ARO o OSD — OpenShift completamente gestito senza provisioning hardware richiesto"
    }
  }
  ```

  validation additions:

  ```json
  "negativePods": "Il numero di pod non puo essere negativo",
  "workerRamBelowMin": "La RAM del nodo worker e inferiore al minimo di 8 GB — il dimensionamento potrebbe non essere valido",
  "workerCpuBelowMin": "Il vCPU del nodo worker e inferiore al minimo di 2 vCPU — il dimensionamento potrebbe non essere valido",
  "zeroPodsWithResources": "Il numero di pod e zero ma le richieste di CPU/RAM sono impostate — verificare il profilo del carico di lavoro",
  "odfIncompatibleTopology": "L'archiviazione ODF non e compatibile con le topologie SNO, MicroShift o Cloud gestito",
  "rhacmNoClusters": "RHACM e abilitato ma non e impostato nessun numero di cluster gestiti — inserire il numero di cluster previsto",
  "techPreviewNotForProduction": "Anteprima tecnologica — non supportata per uso in produzione",
  "redfishBmcRequired": "Richiede BMC compatibile con Redfish su tutti i nodi del cluster",
  "managedCloudNoHardware": "Topologia cloud gestita — nessuna distinta materiali hardware viene generata"
  ```

  For each locale file: read the current file, add the missing keys to their correct JSON sections (merging into existing objects, adding new top-level objects), then write the complete updated file.

  Verify each file is valid JSON after writing.
  </action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/os-sizer && for f in src/i18n/locales/fr.json src/i18n/locales/de.json src/i18n/locales/it.json; do node -e "JSON.parse(require('fs').readFileSync('$f','utf8')); console.log('$f: valid')"; done && npm run type-check 2>&1 | tail -5 && npm run test 2>&1 | tail -5</automated>
  </verify>
  <done>

- fr.json, de.json, it.json are valid JSON
- All 3 files contain recommendation.*, validation.negativePods and all other validation keys
- All 3 files contain warnings.tna.*, warnings.tnf.*, warnings.managedCloud.*
- All 3 files contain workload.nodeVcpu, workload.nodeRam
- All 3 files contain wizard.step3.selectionRequired, showOverride, hideOverride
- All existing keys in each locale preserved (no regressions)
- npm run type-check exits 0
- npm run test exits 0
  </done>

</task>

</tasks>

<verification>
- `npm run type-check` exits 0
- `npm run test` exits 0
- All 4 locale files are valid JSON:
  `for f in src/i18n/locales/*.json; do node -e "JSON.parse(require('fs').readFileSync('$f','utf8'))"; done`
- All recommendation keys present in en.json:
  `grep -c "recommendation\." src/i18n/locales/en.json` returns 8
- All validation additions present in en.json:
  `grep "negativePods\|workerRamBelowMin\|techPreviewNotForProduction" src/i18n/locales/en.json | wc -l` returns 3
</verification>

<output>
After completion, create `.planning/phases/03-wizard-ui/03-05-SUMMARY.md` with:
- Count of new keys added per locale file
- Any keys that were already present (so plan was accurate)
- Final npm run test and type-check results
</output>

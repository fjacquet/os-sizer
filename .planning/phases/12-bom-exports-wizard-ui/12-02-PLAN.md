---
phase: 12-bom-exports-wizard-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - src/components/wizard/Step2WorkloadForm.vue
autonomous: true
requirements:
  - VIRT-01
  - GPU-01
  - GPU-02
  - GPU-04
  - GPU-05
  - RHOAI-01

must_haves:
  truths:
    - "Checkbox 'OpenShift Virtualization' (virtAddon key) appears in Step2 add-ons section"
    - "Checkbox 'GPU Node Pool' (gpuNodePool key) appears in Step2 add-ons section"
    - "GPU sub-inputs (gpuNodeCount slider, gpuMode select, gpuModel select, migProfile select) appear when gpuEnabled is true"
    - "vGPU density reference table appears when gpuMode === 'vgpu' with disclaimer note"
    - "Checkbox 'RHOAI' (rhoaiAddon key) appears in Step2 add-ons section"
    - "All new inputs use addOnField() helper — sibling addOns fields are never wiped"
    - "TypeScript compiles and all tests pass"
  artifacts:
    - path: "src/components/wizard/Step2WorkloadForm.vue"
      provides: "Virt, GPU, RHOAI add-on toggles and GPU sub-inputs"
      contains: "gpuEnabled"
  key_links:
    - from: "Step2WorkloadForm.vue addOnField('virtEnabled')"
      to: "inputStore.updateCluster addOns spread"
      via: "addOnField computed setter with { ...c.addOns, virtEnabled: val }"
      pattern: "addOnField"
    - from: "Step2WorkloadForm.vue addOnField('gpuEnabled')"
      to: "inputStore.updateCluster addOns spread"
      via: "addOnField computed setter"
      pattern: "addOnField"
---

<objective>
Add Virtualization, GPU, and RHOAI toggles plus GPU configuration sub-inputs to Step2WorkloadForm.vue.

Purpose: Exposes all three new v2.0 add-ons through the wizard's workload step. The Virt and RHOAI toggles are simple checkboxes. The GPU section has a checkbox plus a revealed sub-input block with node count, GPU mode, GPU model, and MIG profile selects — exactly matching the RHACM pattern.

Output: Updated Step2WorkloadForm.vue with new add-on section entries and GPU sub-inputs block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-bom-exports-wizard-ui/12-01-SUMMARY.md

<interfaces>
<!-- Key patterns from Step2WorkloadForm.vue (read from codebase) -->

Existing addOnField helper (lines 28-36):
```typescript
function addOnField(key: keyof typeof activeCluster.value.addOns) {
  return computed({
    get: () => activeCluster.value.addOns[key],
    set: (val: boolean | number) => {
      const c = input.clusters[input.activeClusterIndex]
      if (c) input.updateCluster(c.id, { addOns: { ...c.addOns, [key]: val } })
    },
  })
}
```

Existing RHACM pattern (template lines 152-172) — model for GPU:
```html
<label class="flex items-center gap-2 cursor-pointer">
  <input type="checkbox" :checked="rhacmEnabled as boolean"
    :aria-label="t('workload.rhacmHub')"
    class="w-4 h-4 accent-blue-600"
    @change="rhacmEnabled = ($event.target as HTMLInputElement).checked" />
  <span class="text-sm text-gray-700 dark:text-gray-300">{{ t('workload.rhacmHub') }}</span>
</label>
<div v-if="rhacmEnabled" class="ml-6 mt-2">
  <NumberSliderInput :model-value="rhacmManagedClusters as number"
    @update:model-value="(val: number) => { rhacmManagedClusters = val }"
    :label="t('hcp.clusterCount')" :min="1" :max="500" :step="1" />
</div>
```

AddOnConfig fields available for addOnField():
- virtEnabled: boolean
- gpuEnabled: boolean
- gpuNodeCount: number
- gpuMode: 'container' | 'passthrough' | 'vgpu'
- gpuModel: 'A100-40GB' | 'A100-80GB' | 'H100-80GB'
- migProfile: string ('' = no MIG)
- rhoaiEnabled: boolean

MIG_PROFILES lookup (from src/engine/constants.ts) — import to drive cascade select:
```typescript
export const MIG_PROFILES: Readonly<Record<string, Readonly<Record<string, number>>>> = {
  'A100-40GB': { '1g.5gb': 7, '2g.10gb': 3, '3g.20gb': 2, '7g.40gb': 1 },
  'A100-80GB': { '1g.10gb': 7, '2g.20gb': 3, '3g.40gb': 2, '7g.80gb': 1 },
  'H100-80GB': { '1g.10gb': 7, '2g.20gb': 3, '3g.40gb': 2, '7g.80gb': 1 },
}
```

New i18n keys available after 12-01:
- t('workload.virtAddon') — "OpenShift Virtualization"
- t('workload.gpuNodePool') — "GPU Node Pool"
- t('workload.gpuNodeCount') — "GPU Node Count"
- t('workload.gpuMode') — "GPU Mode"
- t('workload.gpuModel') — "GPU Model"
- t('workload.migProfile') — "MIG Profile"
- t('gpu.modeContainer') — "Container (whole GPU per pod)"
- t('gpu.modePassthrough') — "Passthrough (whole GPU per VM)"
- t('gpu.modeVgpu') — "vGPU (shared across VMs)"
- t('gpu.migNone') — "No MIG partitioning"
- t('gpu.densityTableTitle') — "vGPU Density Reference..."
- t('gpu.densityNote') — disclaimer
- t('workload.rhoaiAddon') — "Red Hat OpenShift AI (RHOAI)"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Virt, GPU, and RHOAI toggles + GPU sub-inputs to Step2</name>
  <files>src/components/wizard/Step2WorkloadForm.vue</files>
  <action>
    **Script setup changes:**

    Add import at top of script:
    ```typescript
    import { MIG_PROFILES } from '@/engine/constants'
    ```

    Add new addOnField refs after the existing `rhacmManagedClusters` line:
    ```typescript
    const virtEnabled = addOnField('virtEnabled')
    const gpuEnabled = addOnField('gpuEnabled')
    const gpuNodeCount = addOnField('gpuNodeCount')
    const gpuMode = addOnField('gpuMode')
    const gpuModel = addOnField('gpuModel')
    const migProfile = addOnField('migProfile')
    const rhoaiEnabled = addOnField('rhoaiEnabled')
    ```

    Add a computed for available MIG profiles based on selected model:
    ```typescript
    const availableMigProfiles = computed(() =>
      gpuModel.value ? Object.keys(MIG_PROFILES[gpuModel.value as string] ?? {}) : []
    )
    ```

    **Template changes — add-ons section:**

    After the existing RHACM managed-clusters div (after closing `</div>` of the rhacmEnabled sub-input block), add three new blocks inside the `<div class="space-y-2">` add-ons container:

    **1. OpenShift Virtualization checkbox (VIRT-01 / RHOAI-01 per D-pattern):**
    ```html
    <!-- OpenShift Virtualization add-on (VIRT-01) -->
    <label class="flex items-center gap-2 cursor-pointer">
      <input
        type="checkbox"
        :checked="virtEnabled as boolean"
        :aria-label="t('workload.virtAddon')"
        class="w-4 h-4 accent-blue-600"
        @change="virtEnabled = ($event.target as HTMLInputElement).checked"
      />
      <span class="text-sm text-gray-700 dark:text-gray-300">{{ t('workload.virtAddon') }}</span>
    </label>
    ```

    **2. GPU Node Pool checkbox + sub-inputs (GPU-01, GPU-02, GPU-04, GPU-05):**
    ```html
    <!-- GPU Node Pool add-on (GPU-01) -->
    <label class="flex items-center gap-2 cursor-pointer">
      <input
        type="checkbox"
        :checked="gpuEnabled as boolean"
        :aria-label="t('workload.gpuNodePool')"
        class="w-4 h-4 accent-blue-600"
        @change="gpuEnabled = ($event.target as HTMLInputElement).checked"
      />
      <span class="text-sm text-gray-700 dark:text-gray-300">{{ t('workload.gpuNodePool') }}</span>
    </label>
    <!-- GPU sub-inputs revealed when gpuEnabled -->
    <div v-if="gpuEnabled" class="ml-6 mt-2 space-y-3">
      <NumberSliderInput
        :model-value="gpuNodeCount as number"
        @update:model-value="(val: number) => { gpuNodeCount = val }"
        :label="t('workload.gpuNodeCount')"
        :min="1"
        :max="32"
        :step="1"
      />
      <!-- GPU Mode select (GPU-02) -->
      <div class="space-y-1">
        <label class="text-sm font-medium text-gray-700 dark:text-gray-300">{{ t('workload.gpuMode') }}</label>
        <select
          :value="gpuMode as string"
          :aria-label="t('workload.gpuMode')"
          class="w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded text-sm bg-white dark:bg-gray-700 text-gray-900 dark:text-gray-100 focus:ring-2 focus:ring-blue-500 focus:outline-none"
          @change="gpuMode = ($event.target as HTMLSelectElement).value"
        >
          <option value="container">{{ t('gpu.modeContainer') }}</option>
          <option value="passthrough">{{ t('gpu.modePassthrough') }}</option>
          <option value="vgpu">{{ t('gpu.modeVgpu') }}</option>
        </select>
      </div>
      <!-- GPU Model select -->
      <div class="space-y-1">
        <label class="text-sm font-medium text-gray-700 dark:text-gray-300">{{ t('workload.gpuModel') }}</label>
        <select
          :value="gpuModel as string"
          :aria-label="t('workload.gpuModel')"
          class="w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded text-sm bg-white dark:bg-gray-700 text-gray-900 dark:text-gray-100 focus:ring-2 focus:ring-blue-500 focus:outline-none"
          @change="gpuModel = ($event.target as HTMLSelectElement).value"
        >
          <option value="A100-40GB">A100-40GB</option>
          <option value="A100-80GB">A100-80GB</option>
          <option value="H100-80GB">H100-80GB</option>
        </select>
      </div>
      <!-- MIG Profile cascade select (GPU-04) — only for A100/H100 -->
      <div class="space-y-1">
        <label class="text-sm font-medium text-gray-700 dark:text-gray-300">{{ t('workload.migProfile') }}</label>
        <select
          :value="migProfile as string"
          :aria-label="t('workload.migProfile')"
          class="w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded text-sm bg-white dark:bg-gray-700 text-gray-900 dark:text-gray-100 focus:ring-2 focus:ring-blue-500 focus:outline-none"
          @change="migProfile = ($event.target as HTMLSelectElement).value"
        >
          <option value="">{{ t('gpu.migNone') }}</option>
          <option v-for="profile in availableMigProfiles" :key="profile" :value="profile">{{ profile }}</option>
        </select>
      </div>
      <!-- GPU Passthrough live-migration warning (inline UX hint, pitfall 4 avoidance) -->
      <div
        v-if="gpuMode === 'passthrough'"
        class="p-2 rounded border text-xs bg-amber-50 dark:bg-amber-900/30 border-amber-400 dark:border-amber-700 text-amber-800 dark:text-amber-300"
        role="alert"
      >
        {{ t('warnings.gpu.passthroughBlocksLiveMigration') }}
      </div>
      <!-- vGPU density reference table (GPU-05) — only when vgpu mode selected -->
      <div v-if="gpuMode === 'vgpu'" class="mt-2 space-y-1">
        <p class="text-xs font-semibold text-gray-700 dark:text-gray-300">{{ t('gpu.densityTableTitle') }}</p>
        <div class="overflow-x-auto">
          <table class="w-full text-xs border-collapse border border-gray-200 dark:border-gray-600">
            <thead class="bg-gray-50 dark:bg-gray-700">
              <tr>
                <th class="px-2 py-1 border border-gray-200 dark:border-gray-600 text-left">GPU</th>
                <th class="px-2 py-1 border border-gray-200 dark:border-gray-600 text-left">Profile</th>
                <th class="px-2 py-1 border border-gray-200 dark:border-gray-600 text-right">Instances</th>
              </tr>
            </thead>
            <tbody>
              <tr v-for="(instances, profile) in MIG_PROFILES['A100-40GB']" :key="`a100-40-${profile}`">
                <td class="px-2 py-1 border border-gray-200 dark:border-gray-600 text-gray-600 dark:text-gray-400">A100-40GB</td>
                <td class="px-2 py-1 border border-gray-200 dark:border-gray-600 font-mono">{{ profile }}</td>
                <td class="px-2 py-1 border border-gray-200 dark:border-gray-600 font-mono text-right">{{ instances }}</td>
              </tr>
              <tr v-for="(instances, profile) in MIG_PROFILES['A100-80GB']" :key="`a100-80-${profile}`">
                <td class="px-2 py-1 border border-gray-200 dark:border-gray-600 text-gray-600 dark:text-gray-400">A100-80GB</td>
                <td class="px-2 py-1 border border-gray-200 dark:border-gray-600 font-mono">{{ profile }}</td>
                <td class="px-2 py-1 border border-gray-200 dark:border-gray-600 font-mono text-right">{{ instances }}</td>
              </tr>
              <tr v-for="(instances, profile) in MIG_PROFILES['H100-80GB']" :key="`h100-${profile}`">
                <td class="px-2 py-1 border border-gray-200 dark:border-gray-600 text-gray-600 dark:text-gray-400">H100-80GB</td>
                <td class="px-2 py-1 border border-gray-200 dark:border-gray-600 font-mono">{{ profile }}</td>
                <td class="px-2 py-1 border border-gray-200 dark:border-gray-600 font-mono text-right">{{ instances }}</td>
              </tr>
            </tbody>
          </table>
        </div>
        <p class="text-xs text-gray-500 dark:text-gray-400 italic">{{ t('gpu.densityNote') }}</p>
      </div>
    </div>
    ```

    **3. RHOAI checkbox (RHOAI-01):**
    ```html
    <!-- RHOAI add-on (RHOAI-01) -->
    <label class="flex items-center gap-2 cursor-pointer">
      <input
        type="checkbox"
        :checked="rhoaiEnabled as boolean"
        :aria-label="t('workload.rhoaiAddon')"
        class="w-4 h-4 accent-blue-600"
        @change="rhoaiEnabled = ($event.target as HTMLInputElement).checked"
      />
      <span class="text-sm text-gray-700 dark:text-gray-300">{{ t('workload.rhoaiAddon') }}</span>
    </label>
    ```

    ORDERING in add-ons section: ODF, Infra Nodes, RHACM (+ managed-clusters sub), OpenShift Virtualization, GPU Node Pool (+ GPU sub-inputs), RHOAI.

    ANTI-PATTERN: Never pass `{ addOns: { gpuEnabled: true } }` — always use addOnField() which applies the spread pattern internally.
  </action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/os-sizer && npx tsc --noEmit && npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <done>Step2WorkloadForm.vue renders virtAddon checkbox, gpuNodePool checkbox with gpuNodeCount/gpuMode/gpuModel/migProfile sub-inputs (hidden when gpuEnabled=false), vGPU density table (hidden when gpuMode!='vgpu'), passthrough warning (hidden when gpuMode!='passthrough'), rhoaiAddon checkbox. tsc --noEmit exits 0. All tests pass.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0
- `npx vitest run` reports all tests pass
- `grep -c "addOnField" src/components/wizard/Step2WorkloadForm.vue` returns at least 10 (original 4 + 7 new)
- `grep "virtEnabled\|gpuEnabled\|rhoaiEnabled" src/components/wizard/Step2WorkloadForm.vue` returns hits
- `grep "MIG_PROFILES" src/components/wizard/Step2WorkloadForm.vue` returns a hit
</verification>

<success_criteria>
1. virtEnabled checkbox present in Step2 add-ons section (VIRT-01)
2. gpuEnabled checkbox present; reveals sub-inputs with gpuNodeCount, gpuMode, gpuModel, migProfile when checked (GPU-01, GPU-02, GPU-04)
3. vGPU density table visible when gpuMode === 'vgpu' with disclaimer (GPU-05)
4. Passthrough inline warning visible when gpuMode === 'passthrough'
5. rhoaiEnabled checkbox present in Step2 add-ons section (RHOAI-01)
6. All inputs use addOnField() — no direct store mutation
7. tsc --noEmit exits 0; all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/12-bom-exports-wizard-ui/12-02-SUMMARY.md`
</output>

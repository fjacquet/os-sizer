---
phase: 12-bom-exports-wizard-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engine/types.ts
  - src/engine/addons.ts
  - src/i18n/locales/en.json
  - src/i18n/locales/fr.json
  - src/i18n/locales/de.json
  - src/i18n/locales/it.json
autonomous: true
requirements:
  - RHOAI-04
  - RHOAI-01
  - VIRT-01
  - GPU-01
  - GPU-02
  - GPU-03
  - GPU-04
  - GPU-05

must_haves:
  truths:
    - "ClusterSizing has rhoaiOverhead: { vcpu: number; ramGB: number } | null"
    - "calcRHOAI() sets sizing.rhoaiOverhead to { vcpu: RHOAI_INFRA_OVERHEAD_VCPU, ramGB: RHOAI_INFRA_OVERHEAD_RAM_GB } when rhoaiEnabled, null when disabled"
    - "All 5 pre-existing missing i18n keys present in EN, FR, DE, IT locale files"
    - "TypeScript compiles with tsc --noEmit and all 237 existing tests still pass"
  artifacts:
    - path: "src/engine/types.ts"
      provides: "rhoaiOverhead field on ClusterSizing"
      contains: "rhoaiOverhead"
    - path: "src/engine/addons.ts"
      provides: "calcRHOAI sets rhoaiOverhead"
      contains: "rhoaiOverhead"
    - path: "src/i18n/locales/en.json"
      provides: "All 5 deferred warning/recommendation keys"
      contains: "warnings.virt.rwxRequiresOdf"
    - path: "src/i18n/locales/fr.json"
      provides: "FR translations for all 5 keys"
    - path: "src/i18n/locales/de.json"
      provides: "DE translations for all 5 keys"
    - path: "src/i18n/locales/it.json"
      provides: "IT translations for all 5 keys"
  key_links:
    - from: "src/engine/addons.ts"
      to: "src/engine/types.ts"
      via: "ClusterSizing.rhoaiOverhead mutation"
      pattern: "sizing\\.rhoaiOverhead"
---

<objective>
Add the `rhoaiOverhead` field to `ClusterSizing`, update `calcRHOAI()` to populate it, and add all 5 pre-existing deferred i18n keys to all four locale files.

Purpose: This is the blocking Wave 1 foundation. The `rhoaiOverhead` field on `ClusterSizing` is the data contract that Wave 3's BomTable and export plans depend on. The 5 missing i18n keys have been referenced in validation.ts and recommendation.ts since Phases 9-11 but were deferred to Phase 12 — they must land first to prevent runtime key-string rendering for any user who triggers a validation warning.

Output: Updated types.ts, updated addons.ts (calcRHOAI mutation), and 4 locale files with all Phase 12 i18n keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types the executor needs. Extracted from src/engine/types.ts -->

Current ClusterSizing (lines 72-82):
```typescript
export interface ClusterSizing {
  masterNodes: NodeSpec
  workerNodes: NodeSpec | null
  infraNodes: NodeSpec | null
  odfNodes: NodeSpec | null
  rhacmWorkers: NodeSpec | null
  virtWorkerNodes: NodeSpec | null  // Phase 9
  gpuNodes: NodeSpec | null         // Phase 10
  virtStorageGB: number
  totals: { vcpu: number; ramGB: number; storageGB: number }
}
```

Current calcRHOAI signature (src/engine/addons.ts lines 185-205):
```typescript
export function calcRHOAI(sizing: ClusterSizing, infraNodesEnabled: boolean): void {
  if (sizing.workerNodes) {
    sizing.workerNodes = { ...sizing.workerNodes,
      vcpu:  Math.max(sizing.workerNodes.vcpu,  RHOAI_WORKER_MIN_VCPU),
      ramGB: Math.max(sizing.workerNodes.ramGB, RHOAI_WORKER_MIN_RAM_GB),
    }
  }
  if (infraNodesEnabled && sizing.infraNodes) {
    sizing.infraNodes = { ...sizing.infraNodes,
      vcpu:  sizing.infraNodes.vcpu  + RHOAI_INFRA_OVERHEAD_VCPU,
      ramGB: sizing.infraNodes.ramGB + RHOAI_INFRA_OVERHEAD_RAM_GB,
    }
  }
}
```

RHOAI constants (src/engine/constants.ts):
```typescript
export const RHOAI_INFRA_OVERHEAD_VCPU = 4
export const RHOAI_INFRA_OVERHEAD_RAM_GB = 16
```

5 pre-existing missing keys (referenced in validation.ts + recommendation.ts):
- warnings.virt.rwxRequiresOdf      (WARN-02, Phase 9)
- warnings.sno.virtNoLiveMigration  (Phase 9)
- warnings.gpu.passthroughBlocksLiveMigration  (WARN-01, Phase 10)
- warnings.gpu.migProfileWithKubevirtUnsupported (WARN-03, Phase 10)
- recommendation.standardHa.virtWorkloads (Phase 9 justificationKey)

Current en.json "warnings" section ends with managedCloud block (no virt/gpu/sno.virtNoLiveMigration).
Current en.json "recommendation" section — check for standardHa.virtWorkloads absence.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add rhoaiOverhead to ClusterSizing and update calcRHOAI()</name>
  <files>src/engine/types.ts, src/engine/addons.ts</files>
  <behavior>
    - Test 1: calcRHOAI with rhoaiEnabled=true, infraNodesEnabled=true sets sizing.rhoaiOverhead = { vcpu: 4, ramGB: 16 }
    - Test 2: calcRHOAI with rhoaiEnabled=false (caller does not call calcRHOAI) — rhoaiOverhead remains null (set during topology init)
    - Test 3: Existing 237 tests continue to pass — ClusterSizing fixtures in tests must compile even without rhoaiOverhead (it is nullable)
  </behavior>
  <action>
    In src/engine/types.ts: Add `rhoaiOverhead: { vcpu: number; ramGB: number } | null` to ClusterSizing, after the `virtStorageGB` line (before `totals`). Default is null — topology calculators do not set it; only calcRHOAI does.

    In src/engine/addons.ts: Update calcRHOAI() to set `sizing.rhoaiOverhead` at the end of the function body. When infraNodesEnabled is true and sizing.infraNodes is non-null, set rhoaiOverhead to { vcpu: RHOAI_INFRA_OVERHEAD_VCPU, ramGB: RHOAI_INFRA_OVERHEAD_RAM_GB }. When that condition is false (no infra nodes), the overhead lands on workers — set rhoaiOverhead to the same constants. Always set the field when calcRHOAI is called (it is only called when rhoaiEnabled=true). Do NOT change the existing worker-floor or infra-addend logic.

    In src/engine/calculators.ts: Verify that every topology function that creates a ClusterSizing object now includes `rhoaiOverhead: null` in its return value. Search for all `ClusterSizing` object literals and add the field. This prevents TypeScript compile errors from the new required field. (Field is NOT optional — use `null` as the absent value to keep BomTable null-check guards consistent.)

    CRITICAL: Do not make rhoaiOverhead optional (?:) — make it required with `| null` type. This forces all topology calculators to explicitly acknowledge the field.
  </action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/os-sizer && npx tsc --noEmit && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>tsc --noEmit exits 0; all 237+ tests pass; grep confirms rhoaiOverhead appears in types.ts and addons.ts</done>
</task>

<task type="auto">
  <name>Task 2: Add all Phase 12 i18n keys to all 4 locale files</name>
  <files>
    src/i18n/locales/en.json,
    src/i18n/locales/fr.json,
    src/i18n/locales/de.json,
    src/i18n/locales/it.json
  </files>
  <action>
    Add the following key groups to ALL FOUR locale files simultaneously. Missing any locale causes runtime key-string rendering.

    **Group A: 5 pre-existing deferred keys (MUST land in Wave 1)**

    Under "warnings" (add after existing "managedCloud" block):
    ```json
    "virt": {
      "rwxRequiresOdf": "[locale translation]"
    },
    "sno": {
      "virtNoLiveMigration": "[locale translation]"
    },
    "gpu": {
      "passthroughBlocksLiveMigration": "[locale translation]",
      "migProfileWithKubevirtUnsupported": "[locale translation]"
    }
    ```

    Under "recommendation" (add standardHa.virtWorkloads):
    ```json
    "standardHa": {
      "virtWorkloads": "[locale translation]"
    }
    ```

    **Group B: New Phase 12 UI keys**

    Under "node" (add after existing keys):
    ```json
    "virtWorkers": "[locale]",
    "gpu": "[locale]",
    "rhoaiOverhead": "[locale]"
    ```

    Under "workload" (add after existing keys):
    ```json
    "rhoaiAddon": "[locale]",
    "gpuNodePool": "[locale]",
    "gpuNodeCount": "[locale]",
    "gpuMode": "[locale]",
    "gpuModel": "[locale]",
    "migProfile": "[locale]",
    "vmCount": "[locale]",
    "vmsPerWorker": "[locale]",
    "virtAvgVmVcpu": "[locale]",
    "virtAvgVmRamGB": "[locale]",
    "virtAddon": "[locale]"
    ```

    Add new top-level "gpu" section:
    ```json
    "gpu": {
      "modeContainer": "[locale]",
      "modePassthrough": "[locale]",
      "modeVgpu": "[locale]",
      "migNone": "[locale]",
      "densityTableTitle": "[locale]",
      "densityNote": "[locale]"
    }
    ```

    Add new top-level "rhoai" section:
    ```json
    "rhoai": {
      "label": "[locale]",
      "bomRow": "[locale]",
      "kserve": "[locale]",
      "dsPipelines": "[locale]",
      "modelRegistry": "[locale]"
    }
    ```

    Add to "sno" section (add snoVirtMode key after existing keys):
    ```json
    "virtMode": "[locale]"
    ```

    **English (en.json) reference translations:**
    - warnings.virt.rwxRequiresOdf: "OpenShift Virtualization requires RWX storage (ODF) for live migration. Without ODF, live migration is unavailable."
    - warnings.sno.virtNoLiveMigration: "SNO topology does not support live migration — VM HA is unavailable."
    - warnings.gpu.passthroughBlocksLiveMigration: "GPU passthrough mode permanently blocks live migration on affected nodes."
    - warnings.gpu.migProfileWithKubevirtUnsupported: "MIG profiles combined with KubeVirt VMs are unsupported by the standard GPU Operator."
    - recommendation.standardHa.virtWorkloads: "Recommended for VM workloads — standard HA provides live migration and node redundancy."
    - node.virtWorkers: "Virt Worker Nodes"
    - node.gpu: "GPU Nodes"
    - node.rhoaiOverhead: "RHOAI Overhead"
    - workload.rhoaiAddon: "Red Hat OpenShift AI (RHOAI)"
    - workload.gpuNodePool: "GPU Node Pool"
    - workload.gpuNodeCount: "GPU Node Count"
    - workload.gpuMode: "GPU Mode"
    - workload.gpuModel: "GPU Model"
    - workload.migProfile: "MIG Profile"
    - workload.vmCount: "Total VM Count"
    - workload.vmsPerWorker: "VMs per Worker Node"
    - workload.virtAvgVmVcpu: "Average VM vCPU"
    - workload.virtAvgVmRamGB: "Average VM RAM (GB)"
    - workload.virtAddon: "OpenShift Virtualization"
    - gpu.modeContainer: "Container (whole GPU per pod)"
    - gpu.modePassthrough: "Passthrough (whole GPU per VM)"
    - gpu.modeVgpu: "vGPU (shared across VMs)"
    - gpu.migNone: "No MIG partitioning"
    - gpu.densityTableTitle: "vGPU Density Reference (estimated, driver-dependent)"
    - gpu.densityNote: "Values are estimates. Actual density depends on NVIDIA vGPU driver version and license tier."
    - rhoai.label: "Red Hat OpenShift AI"
    - rhoai.bomRow: "RHOAI Operator Overhead"
    - rhoai.kserve: "KServe"
    - rhoai.dsPipelines: "Data Science Pipelines"
    - rhoai.modelRegistry: "Model Registry"
    - sno.virtMode: "SNO with Virtualization Profile"

    **FR (fr.json) translations:**
    - warnings.virt.rwxRequiresOdf: "OpenShift Virtualization nécessite du stockage RWX (ODF) pour la migration en direct. Sans ODF, la migration en direct est indisponible."
    - warnings.sno.virtNoLiveMigration: "La topologie SNO ne prend pas en charge la migration en direct — la haute disponibilité des VM est indisponible."
    - warnings.gpu.passthroughBlocksLiveMigration: "Le mode passthrough GPU bloque définitivement la migration en direct sur les nœuds concernés."
    - warnings.gpu.migProfileWithKubevirtUnsupported: "Les profils MIG combinés avec les VM KubeVirt ne sont pas pris en charge par l'opérateur GPU standard."
    - recommendation.standardHa.virtWorkloads: "Recommandé pour les charges de travail VM — la haute disponibilité standard fournit la migration en direct et la redondance des nœuds."
    - node.virtWorkers: "Nœuds Workers Virt"
    - node.gpu: "Nœuds GPU"
    - node.rhoaiOverhead: "Surcharge RHOAI"
    - workload.rhoaiAddon: "Red Hat OpenShift AI (RHOAI)"
    - workload.gpuNodePool: "Pool de nœuds GPU"
    - workload.gpuNodeCount: "Nombre de nœuds GPU"
    - workload.gpuMode: "Mode GPU"
    - workload.gpuModel: "Modèle GPU"
    - workload.migProfile: "Profil MIG"
    - workload.vmCount: "Nombre total de VM"
    - workload.vmsPerWorker: "VM par nœud worker"
    - workload.virtAvgVmVcpu: "vCPU moyen par VM"
    - workload.virtAvgVmRamGB: "RAM moyenne par VM (Go)"
    - workload.virtAddon: "OpenShift Virtualization"
    - gpu.modeContainer: "Conteneur (GPU entier par pod)"
    - gpu.modePassthrough: "Passthrough (GPU entier par VM)"
    - gpu.modeVgpu: "vGPU (partagé entre VM)"
    - gpu.migNone: "Pas de partitionnement MIG"
    - gpu.densityTableTitle: "Référence de densité vGPU (estimée, dépendante du pilote)"
    - gpu.densityNote: "Les valeurs sont des estimations. La densité réelle dépend de la version du pilote NVIDIA vGPU et du niveau de licence."
    - rhoai.label: "Red Hat OpenShift AI"
    - rhoai.bomRow: "Surcharge opérateur RHOAI"
    - rhoai.kserve: "KServe"
    - rhoai.dsPipelines: "Data Science Pipelines"
    - rhoai.modelRegistry: "Registre de modèles"
    - sno.virtMode: "Profil SNO avec virtualisation"

    **DE (de.json) translations:**
    - warnings.virt.rwxRequiresOdf: "OpenShift Virtualization benötigt RWX-Speicher (ODF) für die Live-Migration. Ohne ODF ist die Live-Migration nicht verfügbar."
    - warnings.sno.virtNoLiveMigration: "SNO-Topologie unterstützt keine Live-Migration — VM-HA ist nicht verfügbar."
    - warnings.gpu.passthroughBlocksLiveMigration: "GPU-Passthrough-Modus blockiert die Live-Migration auf betroffenen Knoten dauerhaft."
    - warnings.gpu.migProfileWithKubevirtUnsupported: "MIG-Profile in Kombination mit KubeVirt-VMs werden vom Standard-GPU-Operator nicht unterstützt."
    - recommendation.standardHa.virtWorkloads: "Empfohlen für VM-Workloads — Standard-HA bietet Live-Migration und Knotenredundanz."
    - node.virtWorkers: "Virt-Worker-Knoten"
    - node.gpu: "GPU-Knoten"
    - node.rhoaiOverhead: "RHOAI-Overhead"
    - workload.rhoaiAddon: "Red Hat OpenShift AI (RHOAI)"
    - workload.gpuNodePool: "GPU-Knoten-Pool"
    - workload.gpuNodeCount: "Anzahl GPU-Knoten"
    - workload.gpuMode: "GPU-Modus"
    - workload.gpuModel: "GPU-Modell"
    - workload.migProfile: "MIG-Profil"
    - workload.vmCount: "Gesamtanzahl VMs"
    - workload.vmsPerWorker: "VMs pro Worker-Knoten"
    - workload.virtAvgVmVcpu: "Durchschnittliche vCPU pro VM"
    - workload.virtAvgVmRamGB: "Durchschnittlicher RAM pro VM (GB)"
    - workload.virtAddon: "OpenShift Virtualization"
    - gpu.modeContainer: "Container (ganzer GPU pro Pod)"
    - gpu.modePassthrough: "Passthrough (ganzer GPU pro VM)"
    - gpu.modeVgpu: "vGPU (gemeinsam genutzt über VMs)"
    - gpu.migNone: "Keine MIG-Partitionierung"
    - gpu.densityTableTitle: "vGPU-Dichte-Referenz (geschätzt, treiberabhängig)"
    - gpu.densityNote: "Werte sind Schätzungen. Die tatsächliche Dichte hängt von der NVIDIA-vGPU-Treiberversion und Lizenzstufe ab."
    - rhoai.label: "Red Hat OpenShift AI"
    - rhoai.bomRow: "RHOAI-Operator-Overhead"
    - rhoai.kserve: "KServe"
    - rhoai.dsPipelines: "Data Science Pipelines"
    - rhoai.modelRegistry: "Modell-Registry"
    - sno.virtMode: "SNO mit Virtualisierungsprofil"

    **IT (it.json) translations:**
    - warnings.virt.rwxRequiresOdf: "OpenShift Virtualization richiede storage RWX (ODF) per la migrazione live. Senza ODF, la migrazione live non è disponibile."
    - warnings.sno.virtNoLiveMigration: "La topologia SNO non supporta la migrazione live — l'HA delle VM non è disponibile."
    - warnings.gpu.passthroughBlocksLiveMigration: "La modalità GPU passthrough blocca permanentemente la migrazione live sui nodi interessati."
    - warnings.gpu.migProfileWithKubevirtUnsupported: "I profili MIG combinati con VM KubeVirt non sono supportati dall'operatore GPU standard."
    - recommendation.standardHa.virtWorkloads: "Consigliato per carichi di lavoro VM — l'HA standard fornisce migrazione live e ridondanza dei nodi."
    - node.virtWorkers: "Nodi Worker Virt"
    - node.gpu: "Nodi GPU"
    - node.rhoaiOverhead: "Overhead RHOAI"
    - workload.rhoaiAddon: "Red Hat OpenShift AI (RHOAI)"
    - workload.gpuNodePool: "Pool di nodi GPU"
    - workload.gpuNodeCount: "Numero di nodi GPU"
    - workload.gpuMode: "Modalità GPU"
    - workload.gpuModel: "Modello GPU"
    - workload.migProfile: "Profilo MIG"
    - workload.vmCount: "Numero totale di VM"
    - workload.vmsPerWorker: "VM per nodo worker"
    - workload.virtAvgVmVcpu: "vCPU media per VM"
    - workload.virtAvgVmRamGB: "RAM media per VM (GB)"
    - workload.virtAddon: "OpenShift Virtualization"
    - gpu.modeContainer: "Container (GPU intero per pod)"
    - gpu.modePassthrough: "Passthrough (GPU intero per VM)"
    - gpu.modeVgpu: "vGPU (condiviso tra VM)"
    - gpu.migNone: "Nessuna partizione MIG"
    - gpu.densityTableTitle: "Riferimento densità vGPU (stimato, dipendente dal driver)"
    - gpu.densityNote: "I valori sono stime. La densità effettiva dipende dalla versione del driver NVIDIA vGPU e dal livello di licenza."
    - rhoai.label: "Red Hat OpenShift AI"
    - rhoai.bomRow: "Overhead operatore RHOAI"
    - rhoai.kserve: "KServe"
    - rhoai.dsPipelines: "Data Science Pipelines"
    - rhoai.modelRegistry: "Registro modelli"
    - sno.virtMode: "Profilo SNO con virtualizzazione"

    CRITICAL: Use proper Swiss German umlauts (ä, ö, ü) throughout de.json — eszett (ß) is forbidden per Phase 05 decision. Use French nœuds ligature for French node-related translations where applicable.
  </action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/os-sizer && npx tsc --noEmit && npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <done>All 4 locale files have warnings.virt, warnings.sno.virtNoLiveMigration, warnings.gpu, recommendation.standardHa.virtWorkloads, node.virtWorkers, node.gpu, node.rhoaiOverhead, workload.rhoaiAddon, workload.gpuNodePool, workload.gpuMode, workload.gpuModel, workload.migProfile, workload.vmCount, workload.vmsPerWorker, workload.virtAvgVmVcpu, workload.virtAvgVmRamGB, workload.virtAddon, gpu.*, rhoai.*, sno.virtMode keys. tsc --noEmit exits 0. All tests pass.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0
- `npx vitest run` reports 237+ passing tests (all prior tests still pass)
- `grep -r "rhoaiOverhead" src/engine/` finds hits in both types.ts and addons.ts
- `grep "rwxRequiresOdf" src/i18n/locales/en.json src/i18n/locales/fr.json src/i18n/locales/de.json src/i18n/locales/it.json` shows 4 hits
- `grep "recommendation.standardHa" src/i18n/locales/en.json` would not work (nested JSON — check that the key exists in the standardHa section)
</verification>

<success_criteria>
1. ClusterSizing.rhoaiOverhead: { vcpu: number; ramGB: number } | null is defined in types.ts
2. calcRHOAI() sets sizing.rhoaiOverhead = { vcpu: RHOAI_INFRA_OVERHEAD_VCPU, ramGB: RHOAI_INFRA_OVERHEAD_RAM_GB } when called
3. All topology calculator ClusterSizing literals include rhoaiOverhead: null (tsc validates this)
4. All 5 pre-existing deferred keys present in all 4 locales
5. All new Phase 12 UI keys present in all 4 locales
6. 237+ tests pass with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/12-bom-exports-wizard-ui/12-01-SUMMARY.md`
</output>

---
phase: 12-bom-exports-wizard-ui
plan: 03
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - src/components/wizard/Step3ArchitectureForm.vue
autonomous: true
requirements:
  - VIRT-01
  - VIRT-03

must_haves:
  truths:
    - "When virtEnabled=true and topology is standard-ha or compact-3node, VM sizing sub-inputs appear in Step3 (vmCount, vmsPerWorker, virtAvgVmVcpu, virtAvgVmRamGB)"
    - "When topology is sno, a snoVirtMode checkbox appears in Step3 sub-inputs (SNO-with-Virt profile)"
    - "All new Step3 inputs use addOnField() helper — sibling addOns fields are never wiped"
    - "TypeScript compiles and all tests pass"
  artifacts:
    - path: "src/components/wizard/Step3ArchitectureForm.vue"
      provides: "Virt VM sizing sub-inputs and snoVirtMode checkbox"
      contains: "virtEnabled"
  key_links:
    - from: "Step3ArchitectureForm.vue virtEnabled sub-inputs block"
      to: "engine/addons.ts calcVirt()"
      via: "vmCount, vmsPerWorker, virtAvgVmVcpu, virtAvgVmRamGB addOnField bindings"
      pattern: "addOnField"
---

<objective>
Add Virtualization VM-sizing sub-inputs and SNO-with-Virt profile toggle to Step3ArchitectureForm.vue.

Purpose: Step3 is the architecture step where topology-specific configuration lives (see HCP sliders, SNO profile buttons). The virt VM inputs belong here per the VIRT-01/VIRT-03 requirements — they appear after topology confirmation, giving users the architectural context that these inputs drive worker node sizing for the selected topology. The snoVirtMode checkbox (SNO-01 engine already complete) needs a UI entry point here.

Output: Updated Step3ArchitectureForm.vue with new virt sub-inputs and snoVirtMode toggle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-bom-exports-wizard-ui/12-01-SUMMARY.md

<interfaces>
<!-- Key patterns from Step3ArchitectureForm.vue (read from codebase) -->

Existing imports (lines 1-11):
```typescript
import { computed, ref } from 'vue'
import { useI18n } from 'vue-i18n'
import { useInputStore } from '@/stores/inputStore'
import { useUiStore } from '@/stores/uiStore'
import { useCalculationStore } from '@/stores/calculationStore'
import { createDefaultClusterConfig } from '@/engine/defaults'
import RecommendationCard from './RecommendationCard.vue'
import NumberSliderInput from '@/components/shared/NumberSliderInput.vue'
import type { TopologyType, SnoProfile, ClusterConfig } from '@/engine/types'
```

Existing clusterField helper (lines 42-50):
```typescript
function clusterField<K extends keyof ClusterConfig>(key: K) {
  return computed({
    get: () => activeCluster.value[key],
    set: (val: ClusterConfig[K]) => {
      const c = input.clusters[input.activeClusterIndex]
      if (c) input.updateCluster(c.id, { [key]: val } as Partial<ClusterConfig>)
    },
  })
}
```

Step3 does NOT have addOnField() — must add it (identical to Step2 version).

Existing topology sub-inputs section (lines 108-185):
```html
<div v-if="ui.topologyConfirmed" class="border-t ...">
  <!-- SNO profile selector (v-if="topology === 'sno'") -->
  <!-- HCP inputs (v-if="topology === 'hcp'") -->
  <!-- TNA notice (v-if="topology === 'two-node-arbiter'") -->
  <!-- TNF notices (v-if="topology === 'two-node-fencing'") -->
  <!-- Managed cloud notice (v-if="topology === 'managed-cloud'") -->
</div>
```

New addOnField keys needed in Step3:
- virtEnabled: boolean (READ ONLY — to conditionally show virt sub-inputs)
- vmCount: number
- vmsPerWorker: number
- virtAvgVmVcpu: number
- virtAvgVmRamGB: number
- snoVirtMode: boolean

Note: virtEnabled is set in Step2. Step3 only reads it to conditionally render sub-inputs, and uses addOnField to bind vmCount etc.

New i18n keys (available after 12-01):
- t('workload.vmCount') — "Total VM Count"
- t('workload.vmsPerWorker') — "VMs per Worker Node"
- t('workload.virtAvgVmVcpu') — "Average VM vCPU"
- t('workload.virtAvgVmRamGB') — "Average VM RAM (GB)"
- t('sno.virtMode') — "SNO with Virtualization Profile"
- t('workload.virtAddon') — "OpenShift Virtualization"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add addOnField helper and virt/SNO sub-inputs to Step3</name>
  <files>src/components/wizard/Step3ArchitectureForm.vue</files>
  <action>
    **Script setup changes:**

    Add addOnField helper after the existing clusterField function (Step3 does not yet have it):
    ```typescript
    function addOnField(key: keyof typeof activeCluster.value.addOns) {
      return computed({
        get: () => activeCluster.value.addOns[key],
        set: (val: boolean | number) => {
          const c = input.clusters[input.activeClusterIndex]
          if (c) input.updateCluster(c.id, { addOns: { ...c.addOns, [key]: val } })
        },
      })
    }
    ```

    Add new addOnField refs after the existing topology/snoProfile/hcpHostedClusters/hcpQpsPerCluster lines:
    ```typescript
    const virtEnabled = addOnField('virtEnabled')
    const vmCount = addOnField('vmCount')
    const vmsPerWorker = addOnField('vmsPerWorker')
    const virtAvgVmVcpu = addOnField('virtAvgVmVcpu')
    const virtAvgVmRamGB = addOnField('virtAvgVmRamGB')
    const snoVirtMode = addOnField('snoVirtMode')
    ```

    **Template changes — topology-specific sub-inputs section:**

    Inside the `<div v-if="ui.topologyConfirmed" ...>` block, add two new conditional blocks:

    **1. Virt VM sizing sub-inputs (VIRT-01, VIRT-03):**
    Add after the HCP inputs block (after the closing `</div>` of `v-if="topology === 'hcp'"`):
    ```html
    <!-- Virt VM sizing inputs (VIRT-01, VIRT-03) — shown when virt add-on enabled on standard-ha or compact-3node -->
    <div v-if="virtEnabled && (topology === 'standard-ha' || topology === 'compact-3node')" class="space-y-3">
      <p class="text-sm font-medium text-gray-700 dark:text-gray-300">{{ t('workload.virtAddon') }}</p>
      <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
        <NumberSliderInput
          :model-value="vmCount as number"
          @update:model-value="(val: number) => { vmCount = val }"
          :label="t('workload.vmCount')"
          :min="1"
          :max="5000"
          :step="10"
          aria-required="true"
        />
        <NumberSliderInput
          :model-value="vmsPerWorker as number"
          @update:model-value="(val: number) => { vmsPerWorker = val }"
          :label="t('workload.vmsPerWorker')"
          :min="1"
          :max="50"
          :step="1"
          aria-required="true"
        />
        <NumberSliderInput
          :model-value="virtAvgVmVcpu as number"
          @update:model-value="(val: number) => { virtAvgVmVcpu = val }"
          :label="t('workload.virtAvgVmVcpu')"
          unit="vCPU"
          :min="1"
          :max="32"
          :step="1"
          aria-required="true"
        />
        <NumberSliderInput
          :model-value="virtAvgVmRamGB as number"
          @update:model-value="(val: number) => { virtAvgVmRamGB = val }"
          :label="t('workload.virtAvgVmRamGB')"
          unit="GB"
          :min="1"
          :max="256"
          :step="1"
          aria-required="true"
        />
      </div>
    </div>
    ```

    **2. SNO-with-Virt profile toggle:**
    Inside the SNO profile block (`v-if="topology === 'sno'"`), add after the profile button group:
    ```html
    <!-- SNO-with-Virt mode toggle (SNO-01 engine already complete) -->
    <div class="mt-3">
      <label class="flex items-center gap-2 cursor-pointer">
        <input
          type="checkbox"
          :checked="snoVirtMode as boolean"
          :aria-label="t('sno.virtMode')"
          class="w-4 h-4 accent-blue-600"
          @change="snoVirtMode = ($event.target as HTMLInputElement).checked"
        />
        <span class="text-sm text-gray-700 dark:text-gray-300">{{ t('sno.virtMode') }}</span>
      </label>
    </div>
    ```

    CRITICAL: Do not add virtEnabled as a TopologyType value. The `topology` computed ref continues to use the existing `clusterField('topology')` — no change. The virt inputs are an overlay on existing topologies, not a new topology type.
  </action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/os-sizer && npx tsc --noEmit && npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <done>Step3ArchitectureForm.vue has addOnField helper, virtEnabled/vmCount/vmsPerWorker/virtAvgVmVcpu/virtAvgVmRamGB/snoVirtMode refs, virt sub-inputs block visible when virtEnabled+standard-ha/compact-3node, snoVirtMode checkbox inside SNO profile block. tsc --noEmit exits 0. All tests pass.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0
- `npx vitest run` reports all tests pass
- `grep "addOnField" src/components/wizard/Step3ArchitectureForm.vue` returns hits
- `grep "virtEnabled\|snoVirtMode" src/components/wizard/Step3ArchitectureForm.vue` returns hits
- `grep "vmCount\|vmsPerWorker" src/components/wizard/Step3ArchitectureForm.vue` returns hits
</verification>

<success_criteria>
1. addOnField helper defined in Step3's script setup
2. vmCount, vmsPerWorker, virtAvgVmVcpu, virtAvgVmRamGB NumberSliderInput components rendered when virtEnabled=true and topology is standard-ha or compact-3node (VIRT-01, VIRT-03)
3. snoVirtMode checkbox rendered inside SNO profile block (SNO topology)
4. No new TopologyType enum values added
5. tsc --noEmit exits 0; all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/12-bom-exports-wizard-ui/12-03-SUMMARY.md`
</output>

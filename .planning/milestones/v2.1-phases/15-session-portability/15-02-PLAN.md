---
phase: 15-session-portability
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - src/components/results/ExportToolbar.vue
  - src/i18n/locales/en.json
  - src/i18n/locales/fr.json
  - src/i18n/locales/de.json
  - src/i18n/locales/it.json
autonomous: true
requirements:
  - SESSION-01
  - SESSION-02

must_haves:
  truths:
    - "User can click Save Session in the export toolbar and receive a .json file download"
    - "User can click Load Session, select a .json file, and see sizing results restored"
    - "User who selects an invalid JSON file sees a user-readable error message and current session is unchanged"
    - "Importing the same file twice works correctly (file input resets after each attempt)"
  artifacts:
    - path: "src/components/results/ExportToolbar.vue"
      provides: "Save Session and Load Session buttons with error/success inline messages"
      contains: "importSession"
    - path: "src/i18n/locales/en.json"
      provides: "English i18n keys for session save/load"
      contains: "saveSession"
    - path: "src/i18n/locales/fr.json"
      provides: "French i18n keys for session save/load"
      contains: "saveSession"
    - path: "src/i18n/locales/de.json"
      provides: "German i18n keys for session save/load"
      contains: "saveSession"
    - path: "src/i18n/locales/it.json"
      provides: "Italian i18n keys for session save/load"
      contains: "saveSession"
  key_links:
    - from: "src/components/results/ExportToolbar.vue"
      to: "src/composables/useSessionExport.ts"
      via: "import { exportSession, importSession }"
      pattern: "import.*exportSession.*importSession.*from.*useSessionExport"
    - from: "src/components/results/ExportToolbar.vue"
      to: "src/i18n/locales/en.json"
      via: "t('results.toolbar.saveSession') and t('results.toolbar.loadSession')"
      pattern: "results\\.toolbar\\.saveSession"
---

<objective>
Wire the useSessionExport composable into ExportToolbar.vue with Save/Load buttons, hidden file input, error/success messages, and add i18n keys to all 4 locale files.

Purpose: Deliver the user-facing session portability feature — the toolbar buttons that call exportSession() and importSession() with proper error handling, file input reset (D-08), and i18n-driven messages (D-07, D-09).

Output: Modified ExportToolbar.vue with two new buttons + inline feedback, and 4 updated locale files with session-related i18n keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-session-portability/15-CONTEXT.md
@.planning/phases/15-session-portability/15-RESEARCH.md
@.planning/phases/15-session-portability/15-UI-SPEC.md
@.planning/phases/15-session-portability/15-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output -->

From src/composables/useSessionExport.ts (created in Plan 01):
```typescript
export function exportSession(): void
export function importSession(file: File): Promise<void>
// importSession rejects with:
//   Error('parse')  — invalid JSON
//   Error('schema') — Zod validation failure
//   Error('read')   — FileReader error
```

From src/components/results/ExportToolbar.vue (current state):
```vue
<script setup lang="ts">
import { ref } from 'vue'
import { useI18n } from 'vue-i18n'
import { generateShareUrl } from '@/composables/useUrlState'
import { generatePptxReport } from '@/composables/usePptxExport'
import { generateCsvReport } from '@/composables/useCsvExport'
import { generatePdfReport } from '@/composables/usePdfExport'

const { t } = useI18n()
const copied = ref(false)
// ... handlers for share, csv, pdf, pptx ...
</script>

<template>
  <div class="flex flex-wrap gap-2 pt-2 print:hidden">
    <!-- Share URL button (line 55) -->
    <!-- CSV button (line 62) -->
    <!-- PDF button (line 70) -->
    <!-- PPTX button (line 79) -->
  </div>
</template>
```

Existing toolbar button class (MUST match exactly):
```
text-sm px-2 py-1 sm:px-3 sm:py-2 font-medium rounded border border-gray-300 dark:border-gray-600 bg-white dark:bg-gray-700 text-gray-700 dark:text-gray-200 hover:bg-gray-50 dark:hover:bg-gray-600 transition-colors
```

Existing i18n toolbar keys in en.json (lines 139-146):
```json
"toolbar": {
  "share": "Share URL",
  "copied": "Copied!",
  "exportCsv": "Export CSV",
  "exportPdf": "Export PDF",
  "exportPptx": "Download PPTX",
  "exportPptxLoading": "Generating..."
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add i18n keys to all 4 locale files</name>
  <files>src/i18n/locales/en.json, src/i18n/locales/fr.json, src/i18n/locales/de.json, src/i18n/locales/it.json</files>
  <read_first>
    src/i18n/locales/en.json
    src/i18n/locales/fr.json
    src/i18n/locales/de.json
    src/i18n/locales/it.json
  </read_first>
  <action>
    Add 4 new keys to the `results.toolbar` section of each locale file, immediately after the existing `"exportPptxLoading": "Generating..."` line (per D-09).

    **en.json** — add after `"exportPptxLoading": "Generating..."`:
    ```json
    "saveSession": "Save Session",
    "loadSession": "Load Session",
    "sessionImportError": "Invalid session file. Please select a valid os-sizer session.",
    "sessionImportSuccess": "Session loaded successfully"
    ```

    **fr.json** — add after `"exportPptxLoading"` value:
    ```json
    "saveSession": "Sauvegarder la session",
    "loadSession": "Charger une session",
    "sessionImportError": "Fichier de session invalide. Veuillez choisir un fichier os-sizer valide.",
    "sessionImportSuccess": "Session chargee avec succes"
    ```

    **de.json** — add after `"exportPptxLoading"` value:
    ```json
    "saveSession": "Sitzung speichern",
    "loadSession": "Sitzung laden",
    "sessionImportError": "Ungueltige Sitzungsdatei. Bitte waehlen Sie eine gueltige os-sizer-Sitzung.",
    "sessionImportSuccess": "Sitzung erfolgreich geladen"
    ```

    **it.json** — add after `"exportPptxLoading"` value:
    ```json
    "saveSession": "Salva sessione",
    "loadSession": "Carica sessione",
    "sessionImportError": "File di sessione non valido. Seleziona un file os-sizer valido.",
    "sessionImportSuccess": "Sessione caricata con successo"
    ```

    IMPORTANT: Ensure the JSON remains valid after editing. The `"exportPptxLoading"` line needs a comma added after its value if it was the last key in the toolbar object.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); ['en','fr','de','it'].forEach(l => { const j=JSON.parse(fs.readFileSync('src/i18n/locales/'+l+'.json','utf8')); const t=j.results.toolbar; if(!t.saveSession||!t.loadSession||!t.sessionImportError||!t.sessionImportSuccess) throw new Error(l+' missing keys'); }); console.log('All 4 locale files have all 4 session keys')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "saveSession" src/i18n/locales/en.json` returns `"saveSession": "Save Session"`
    - `grep "loadSession" src/i18n/locales/en.json` returns `"loadSession": "Load Session"`
    - `grep "sessionImportError" src/i18n/locales/en.json` returns a line containing "Invalid session file"
    - `grep "sessionImportSuccess" src/i18n/locales/en.json` returns a line containing "Session loaded successfully"
    - `grep "saveSession" src/i18n/locales/fr.json` returns a line containing "Sauvegarder"
    - `grep "saveSession" src/i18n/locales/de.json` returns a line containing "Sitzung speichern"
    - `grep "saveSession" src/i18n/locales/it.json` returns a line containing "Salva sessione"
    - All 4 locale files parse as valid JSON: `node -e "JSON.parse(require('fs').readFileSync('src/i18n/locales/en.json'))"` exits 0
  </acceptance_criteria>
  <done>All 4 locale files contain saveSession, loadSession, sessionImportError, sessionImportSuccess keys under results.toolbar with appropriate translations</done>
</task>

<task type="auto">
  <name>Task 2: Wire Save/Load Session buttons into ExportToolbar.vue</name>
  <files>src/components/results/ExportToolbar.vue</files>
  <read_first>
    src/components/results/ExportToolbar.vue
    src/composables/useSessionExport.ts
    .planning/phases/15-session-portability/15-UI-SPEC.md
  </read_first>
  <action>
    Modify `src/components/results/ExportToolbar.vue` to add session save/load functionality.

    **Script section changes:**

    1. Add import at the top (after existing composable imports):
       ```typescript
       import { exportSession, importSession } from '@/composables/useSessionExport'
       ```

    2. Add new refs after existing `const pptxLoading = ref(false)` line:
       ```typescript
       const fileInputKey = ref(0)
       const sessionImportError = ref<string | null>(null)
       const sessionImportSuccess = ref(false)
       ```

    3. Add new handler functions after existing handleExportPptx:
       ```typescript
       function handleSaveSession() {
         exportSession()
       }

       const fileInputRef = ref<HTMLInputElement | null>(null)

       function handleLoadSessionClick() {
         fileInputRef.value?.click()
       }

       async function handleLoadSession(event: Event) {
         const file = (event.target as HTMLInputElement).files?.[0]
         if (!file) return
         sessionImportError.value = null
         sessionImportSuccess.value = false
         try {
           await importSession(file)
           sessionImportSuccess.value = true
           setTimeout(() => {
             sessionImportSuccess.value = false
           }, 1500)
         } catch (err) {
           sessionImportError.value = t('results.toolbar.sessionImportError')
         } finally {
           fileInputKey.value++
         }
       }
       ```

    **Template section changes:**

    Insert the following BEFORE the existing Share URL button (inside the `<div class="flex flex-wrap gap-2 pt-2 print:hidden">`). The button order per UI-SPEC is: Save Session | Load Session | Share URL | CSV | PDF | PPTX.

    ```html
    <!-- Save Session -->
    <button
      class="text-sm px-2 py-1 sm:px-3 sm:py-2 font-medium rounded border border-gray-300 dark:border-gray-600 bg-white dark:bg-gray-700 text-gray-700 dark:text-gray-200 hover:bg-gray-50 dark:hover:bg-gray-600 transition-colors"
      :aria-label="t('results.toolbar.saveSession')"
      @click="handleSaveSession"
    >
      {{ t('results.toolbar.saveSession') }}
    </button>
    <!-- Load Session -->
    <button
      class="text-sm px-2 py-1 sm:px-3 sm:py-2 font-medium rounded border border-gray-300 dark:border-gray-600 bg-white dark:bg-gray-700 text-gray-700 dark:text-gray-200 hover:bg-gray-50 dark:hover:bg-gray-600 transition-colors"
      :aria-label="t('results.toolbar.loadSession')"
      @click="handleLoadSessionClick"
    >
      {{ t('results.toolbar.loadSession') }}
    </button>
    <!-- Hidden file input for session import (D-08: key-based remount) -->
    <input
      ref="fileInputRef"
      :key="fileInputKey"
      type="file"
      accept=".json"
      class="hidden"
      @change="handleLoadSession"
    />
    ```

    After the closing `</div>` of the flex container (or inside it at the end), add the inline error and success messages:

    ```html
    <!-- Session import feedback (D-06, D-07) -->
    <p
      v-if="sessionImportError"
      class="text-sm text-red-700 dark:text-red-400 mt-1 flex items-center gap-1"
      role="alert"
    >
      <span aria-hidden="true">&#10006;</span>
      {{ sessionImportError }}
    </p>
    <p
      v-if="sessionImportSuccess"
      class="text-sm text-green-600 dark:text-green-400 mt-1"
      role="status"
    >
      {{ t('results.toolbar.sessionImportSuccess') }}
    </p>
    ```

    NOTE: The error/success `<p>` elements should be placed AFTER the flex container div (as siblings), or wrap the entire toolbar in an outer div. The simplest approach is to wrap everything in a `<div>` root: the flex row for buttons + the conditional messages below it.

    Key decisions enforced:
    - Per D-06: `sessionImportError` is `ref<string | null>`, cleared on next import attempt
    - Per D-07: Error message comes from `t('results.toolbar.sessionImportError')` — never raw Zod output
    - Per D-08: `:key="fileInputKey"` incremented in finally block ensures same-file re-import works
    - Success flash uses 1500ms setTimeout matching existing `copied` ref pattern
    - `sessionImportError` and `sessionImportSuccess` are mutually exclusive (import clears both before attempting)

    PITFALL: The error `<p>` must use `role="alert"` for screen reader announcement. The success `<p>` must use `role="status"`.
    PITFALL: The hidden file input must use `ref="fileInputRef"` to allow programmatic `.click()` from the Load Session button.
  </action>
  <verify>
    <automated>rtk vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `grep "import.*exportSession.*importSession" src/components/results/ExportToolbar.vue` matches
    - `grep "fileInputKey" src/components/results/ExportToolbar.vue` matches (D-08 key-based remount)
    - `grep "sessionImportError" src/components/results/ExportToolbar.vue` matches (D-06 error ref)
    - `grep "sessionImportSuccess" src/components/results/ExportToolbar.vue` matches (success flash ref)
    - `grep 'role="alert"' src/components/results/ExportToolbar.vue` matches (accessibility)
    - `grep 'role="status"' src/components/results/ExportToolbar.vue` matches (accessibility)
    - `grep 'accept=".json"' src/components/results/ExportToolbar.vue` matches (file input filter)
    - `grep "t('results.toolbar.saveSession')" src/components/results/ExportToolbar.vue` matches (i18n per D-09)
    - `grep "t('results.toolbar.loadSession')" src/components/results/ExportToolbar.vue` matches (i18n per D-09)
    - `grep "t('results.toolbar.sessionImportError')" src/components/results/ExportToolbar.vue` matches (D-07 i18n error)
    - ExportToolbar.vue does NOT contain raw Zod error references (no `result.error.issues`, no `result.error.message`)
    - `rtk vitest run` shows 0 failures across entire suite
  </acceptance_criteria>
  <done>ExportToolbar.vue has Save Session and Load Session buttons in correct order, hidden file input with key-reset, inline error with role="alert", success flash with role="status", all wired to useSessionExport composable; full test suite passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user file selection -> importSession | User-selected .json file parsed and validated before state mutation |
| importSession error -> UI display | Error messages mapped to i18n keys, never raw Zod output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05 | Spoofing | file input accept attribute | accept | `.json` accept hint is advisory only; Zod safeParse is the real gate. No additional MIME check needed. |
| T-15-06 | Information Disclosure | error messages | mitigate | Error messages use i18n keys (D-07) — never expose Zod internals or file content in error display |
| T-15-07 | Tampering | session overwrite without confirmation | accept | Per D-06 / CONTEXT.md: no confirmation dialog; immediate replacement is intentional design. Success flash confirms the action. |
</threat_model>

<verification>
1. `rtk vitest run` — full suite passes
2. Save Session button visible in toolbar before Share URL
3. Load Session button visible in toolbar after Save Session
4. All 4 locale files have the 4 new keys under results.toolbar
5. `grep 'fileInputKey' src/components/results/ExportToolbar.vue` confirms key-reset pattern
</verification>

<success_criteria>
- Save Session button calls exportSession() and triggers .json download
- Load Session button opens file picker, validates with Zod, restores session
- Invalid file shows inline error message (i18n, not raw Zod) with role="alert"
- Valid file shows 1500ms success flash with role="status"
- Same file can be imported twice (fileInputKey increments)
- All 4 locale files have all 4 session-related i18n keys
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/15-session-portability/15-02-SUMMARY.md`
</output>

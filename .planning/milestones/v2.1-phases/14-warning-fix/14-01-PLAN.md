---
phase: 14-warning-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engine/types.ts
  - src/engine/defaults.ts
  - src/engine/validation.ts
  - src/composables/useUrlState.ts
  - src/components/wizard/Step2WorkloadForm.vue
  - src/i18n/locales/en.json
  - src/i18n/locales/fr.json
  - src/i18n/locales/de.json
  - src/i18n/locales/it.json
  - src/engine/validation.test.ts
  - src/composables/__tests__/usePptxExport.test.ts
autonomous: true
requirements:
  - WARN-04
  - WARN-05

must_haves:
  truths:
    - "User enabling virtualization with ODF selected sees no live migration storage warning"
    - "User enabling virtualization with a non-ODF RWX storage class sees no live migration storage warning"
    - "User enabling virtualization with no RWX storage of any kind sees the VIRT_RWX_STORAGE_REQUIRED warning"
    - "Warning key VIRT_RWX_REQUIRES_ODF no longer exists in any source file, locale file, or test fixture"
    - "All 4 locale files carry the updated warning message referencing RWX storage"
  artifacts:
    - path: "src/engine/types.ts"
      provides: "rwxStorageAvailable field on AddOnConfig"
      contains: "rwxStorageAvailable: boolean"
    - path: "src/engine/defaults.ts"
      provides: "Default value for rwxStorageAvailable"
      contains: "rwxStorageAvailable: false"
    - path: "src/engine/validation.ts"
      provides: "Updated guard checking both odfEnabled and rwxStorageAvailable"
      contains: "VIRT_RWX_STORAGE_REQUIRED"
    - path: "src/composables/useUrlState.ts"
      provides: "Zod schema field with backward-compat default"
      contains: "rwxStorageAvailable: z.boolean().optional().default(false)"
    - path: "src/i18n/locales/en.json"
      provides: "Renamed warning key and new checkbox label"
      contains: "rwxStorageRequired"
    - path: "src/engine/validation.test.ts"
      provides: "Updated test cases for new warning code + new rwxStorageAvailable test"
      contains: "VIRT_RWX_STORAGE_REQUIRED"
  key_links:
    - from: "src/engine/validation.ts"
      to: "src/engine/types.ts"
      via: "config.addOns.rwxStorageAvailable field access"
      pattern: "config\\.addOns\\.rwxStorageAvailable"
    - from: "src/composables/useUrlState.ts"
      to: "src/engine/types.ts"
      via: "Zod schema matching AddOnConfig interface"
      pattern: "rwxStorageAvailable.*z\\.boolean"
    - from: "src/components/wizard/Step2WorkloadForm.vue"
      to: "src/engine/types.ts"
      via: "addOnField('rwxStorageAvailable') computed binding"
      pattern: "addOnField.*rwxStorageAvailable"
    - from: "src/i18n/locales/en.json"
      to: "src/engine/validation.ts"
      via: "messageKey 'warnings.virt.rwxStorageRequired' resolved at runtime"
      pattern: "rwxStorageRequired"
---

<objective>
Add `rwxStorageAvailable` boolean field to AddOnConfig, update the WARN-02 validation guard to check both `odfEnabled` and `rwxStorageAvailable`, rename the warning code from `VIRT_RWX_REQUIRES_ODF` to `VIRT_RWX_STORAGE_REQUIRED` across all source, locale, and test files, and add a UI checkbox for the new field.

Purpose: Users with non-ODF RWX storage classes (NFS, Longhorn) currently see a false-positive live migration warning. This fix eliminates that false positive while keeping the warning for users with genuinely no RWX storage.

Output: Updated engine types/defaults/validation, 4 locale files with renamed keys and new messages, updated wizard checkbox, and 7 passing validation tests (6 updated + 1 new).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-warning-fix/14-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add rwxStorageAvailable field, update validation guard, rename warning code, update all locales</name>
  <files>
    src/engine/types.ts,
    src/engine/defaults.ts,
    src/engine/validation.ts,
    src/composables/useUrlState.ts,
    src/i18n/locales/en.json,
    src/i18n/locales/fr.json,
    src/i18n/locales/de.json,
    src/i18n/locales/it.json,
    src/engine/validation.test.ts,
    src/composables/__tests__/usePptxExport.test.ts
  </files>
  <read_first>
    src/engine/types.ts (current AddOnConfig interface — find the field list ending at rhoaiEnabled),
    src/engine/defaults.ts (createDefaultClusterConfig — find addOns object),
    src/engine/validation.ts (lines 37-46 — the WARN-02 guard block; also lines 50+ to confirm SNO block is untouched),
    src/composables/useUrlState.ts (AddOnConfigSchema — find the z.object with rhoaiEnabled),
    src/engine/validation.test.ts (full file — the describe block for WARN-02 starting around line 56),
    src/composables/__tests__/usePptxExport.test.ts (makeClusterConfig function around lines 22-41),
    src/i18n/locales/en.json (warnings.virt section to find rwxRequiresOdf key),
    src/i18n/locales/fr.json (warnings.virt section),
    src/i18n/locales/de.json (warnings.virt section),
    src/i18n/locales/it.json (warnings.virt section)
  </read_first>
  <behavior>
    - Test: virtEnabled=true, odfEnabled=false, rwxStorageAvailable=false, topology=standard-ha -> warning with code 'VIRT_RWX_STORAGE_REQUIRED' emitted
    - Test: virtEnabled=true, odfEnabled=true, rwxStorageAvailable=false -> no VIRT_RWX_STORAGE_REQUIRED warning
    - Test: virtEnabled=false, odfEnabled=false, rwxStorageAvailable=false -> no VIRT_RWX_STORAGE_REQUIRED warning
    - Test: virtEnabled=true, odfEnabled=false, rwxStorageAvailable=false, topology=sno -> no VIRT_RWX_STORAGE_REQUIRED warning (SNO suppression)
    - Test (NEW — WARN-04 core): virtEnabled=true, odfEnabled=false, rwxStorageAvailable=true, topology=standard-ha -> no VIRT_RWX_STORAGE_REQUIRED warning
    - Test: no test assertion references the old code string 'VIRT_RWX_REQUIRES_ODF'
  </behavior>
  <action>
    **Step 1 — Update engine types (src/engine/types.ts):**
    Add `rwxStorageAvailable: boolean` to the `AddOnConfig` interface, after `rhoaiEnabled`. Add a comment: `/** Non-ODF RWX storage class available for live migration (Phase 14) */`

    **Step 2 — Update defaults (src/engine/defaults.ts):**
    Add `rwxStorageAvailable: false` to the `addOns` object inside `createDefaultClusterConfig`, after `rhoaiEnabled: false`.

    **Step 3 — Update Zod schema (src/composables/useUrlState.ts):**
    Add `rwxStorageAvailable: z.boolean().optional().default(false)` to `AddOnConfigSchema`, after `rhoaiEnabled`. The `.optional().default(false)` pattern is required for v2.0 URL backward compatibility (STATE.md note).

    **Step 4 — Update validation guard (src/engine/validation.ts):**
    Replace lines 37-46 (the VIRT_RWX_REQUIRES_ODF block) with:
    ```typescript
    // WARN-04: RWX storage required for live migration
    if (
      config.addOns.virtEnabled &&
      !config.addOns.odfEnabled &&
      !config.addOns.rwxStorageAvailable &&
      config.topology !== 'sno'
    ) {
      warnings.push({
        code: 'VIRT_RWX_STORAGE_REQUIRED',
        severity: 'warning',
        messageKey: 'warnings.virt.rwxStorageRequired',
      })
    }
    ```
    Do NOT touch lines 50+ (SNO suppression block) — leave them exactly as they are.

    **Step 5 — Update locale files:**
    In each of the 4 locale JSON files, inside the `warnings.virt` object:
    - Remove key `"rwxRequiresOdf"`
    - Add key `"rwxStorageRequired"` with the new message text

    Also add a new key in the `workload` section for the checkbox label:
    - `"rwxStorageAvailable"` — label text for the new checkbox

    Exact values for `warnings.virt.rwxStorageRequired`:
    - EN: "OpenShift Virtualization requires RWX-capable storage for live migration. Configure ODF or another RWX storage class to enable live migration."
    - FR: "OpenShift Virtualization necessite un stockage compatible RWX pour la migration en direct. Configurez ODF ou une autre classe de stockage RWX pour activer la migration en direct."
    - DE: "OpenShift Virtualization benotigt RWX-fahigen Speicher fur die Live-Migration. Konfigurieren Sie ODF oder eine andere RWX-Speicherklasse, um die Live-Migration zu aktivieren."
    - IT: "OpenShift Virtualization richiede storage compatibile RWX per la migrazione live. Configurare ODF o un'altra classe di storage RWX per abilitare la migrazione live."

    Exact values for `workload.rwxStorageAvailable`:
    - EN: "RWX Storage Class Available (non-ODF)"
    - FR: "Classe de stockage RWX disponible (non-ODF)"
    - DE: "RWX-Speicherklasse verfugbar (nicht ODF)"
    - IT: "Classe di storage RWX disponibile (non-ODF)"

    NOTE: For DE locale, use proper German characters: "benotigt" should be "benötigt", "fahigen" should be "fähigen", "fur" should be "für", "verfugbar" should be "verfügbar". For FR: "necessite" should be "nécessite".

    **Step 6 — Update test fixtures (src/composables/__tests__/usePptxExport.test.ts):**
    Add `rwxStorageAvailable: false` to the `addOns` object inside `makeClusterConfig()`.

    **Step 7 — Update validation tests (src/engine/validation.test.ts):**
    - Rename the describe block from `'WARN-02: VIRT_RWX_REQUIRES_ODF'` to `'WARN-04/05: VIRT_RWX_STORAGE_REQUIRED'`
    - In every test case within that describe block, ensure the config object includes `config.addOns.rwxStorageAvailable = false` (except in the new test where it is `true`)
    - Change ALL assertion strings from `'VIRT_RWX_REQUIRES_ODF'` to `'VIRT_RWX_STORAGE_REQUIRED'`
    - Add new test case: "no warning when virtEnabled=true, odfEnabled=false, but rwxStorageAvailable=true" — set `config.addOns.virtEnabled = true`, `config.addOns.odfEnabled = false`, `config.addOns.rwxStorageAvailable = true`, topology `'standard-ha'`, assert no warning with code `'VIRT_RWX_STORAGE_REQUIRED'`
  </action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/os-sizer && rtk vitest run src/engine/validation.test.ts && rtk tsc</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "rwxStorageAvailable: boolean" src/engine/types.ts
    - grep -q "rwxStorageAvailable: false" src/engine/defaults.ts
    - grep -q "rwxStorageAvailable: z.boolean().optional().default(false)" src/composables/useUrlState.ts
    - grep -q "VIRT_RWX_STORAGE_REQUIRED" src/engine/validation.ts
    - grep -q "warnings.virt.rwxStorageRequired" src/engine/validation.ts
    - grep -rL "rwxRequiresOdf" src/engine/validation.ts src/i18n/locales/ src/engine/validation.test.ts returns all files (old key absent from all)
    - grep -q "rwxStorageRequired" src/i18n/locales/en.json
    - grep -q "rwxStorageRequired" src/i18n/locales/fr.json
    - grep -q "rwxStorageRequired" src/i18n/locales/de.json
    - grep -q "rwxStorageRequired" src/i18n/locales/it.json
    - grep -q "rwxStorageAvailable" src/i18n/locales/en.json (checkbox label key)
    - grep -q "VIRT_RWX_STORAGE_REQUIRED" src/engine/validation.test.ts
    - grep -c "VIRT_RWX_REQUIRES_ODF" src/engine/validation.test.ts returns 0
    - grep -q "rwxStorageAvailable: false" src/composables/__tests__/usePptxExport.test.ts
    - rtk vitest run src/engine/validation.test.ts passes with 0 failures
    - rtk tsc reports 0 errors
  </acceptance_criteria>
  <done>
    Engine types, defaults, Zod schema, validation guard, all 4 locale files, validation tests, and PPTX test fixture all updated. Old warning code VIRT_RWX_REQUIRES_ODF absent from entire codebase. New code VIRT_RWX_STORAGE_REQUIRED used everywhere. TypeScript compiles clean. All validation tests pass including new rwxStorageAvailable=true test case.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add rwxStorageAvailable checkbox to Step2WorkloadForm</name>
  <files>
    src/components/wizard/Step2WorkloadForm.vue
  </files>
  <read_first>
    src/components/wizard/Step2WorkloadForm.vue (full file — find the addOnField pattern for odfEnabled and virtEnabled, the v-if conditional display pattern, and the checkbox template structure)
  </read_first>
  <action>
    **Step 1 — Add computed in script setup:**
    After the existing `addOnField` declarations (find `virtEnabled` or `odfEnabled` usage), add:
    ```typescript
    const rwxStorageAvailable = addOnField('rwxStorageAvailable')
    ```

    **Step 2 — Add checkbox in template:**
    Find the `virtEnabled` checkbox in the template. Immediately after the closing tag of the virtEnabled checkbox wrapper div, add a conditionally displayed checkbox for `rwxStorageAvailable`:
    ```vue
    <div v-if="virtEnabled" class="ml-6 mt-2">
      <label class="flex items-center gap-2 cursor-pointer">
        <input
          type="checkbox"
          :checked="rwxStorageAvailable as boolean"
          :aria-label="t('workload.rwxStorageAvailable')"
          class="w-4 h-4 accent-blue-600"
          @change="rwxStorageAvailable = ($event.target as HTMLInputElement).checked"
        />
        <span class="text-sm text-gray-700 dark:text-gray-300">{{ t('workload.rwxStorageAvailable') }}</span>
      </label>
    </div>
    ```

    The checkbox is only visible when `virtEnabled` is true (v-if="virtEnabled"), following the same conditional display pattern used by other dependent fields in the form. The `addOnField` computed returns `boolean | number` so the `:checked` binding casts to boolean, matching the odfEnabled checkbox pattern exactly.
  </action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/os-sizer && rtk tsc && rtk vitest run</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "addOnField('rwxStorageAvailable')" src/components/wizard/Step2WorkloadForm.vue
    - grep -q 'v-if="virtEnabled"' src/components/wizard/Step2WorkloadForm.vue (conditional display)
    - grep -q "rwxStorageAvailable as boolean" src/components/wizard/Step2WorkloadForm.vue (checked binding)
    - grep -q "t('workload.rwxStorageAvailable')" src/components/wizard/Step2WorkloadForm.vue (i18n label)
    - rtk tsc reports 0 errors
    - rtk vitest run passes with 0 failures (full test suite)
  </acceptance_criteria>
  <done>
    Step2WorkloadForm.vue has a new checkbox for rwxStorageAvailable that appears only when virtEnabled is true. The checkbox follows the exact same pattern as odfEnabled. Full test suite passes. TypeScript compiles clean.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `rtk vitest run` — full test suite passes (should be 269+ tests, 0 failures)
2. `rtk tsc` — zero TypeScript errors
3. `grep -r "VIRT_RWX_REQUIRES_ODF" src/` — returns zero matches (old code fully removed)
4. `grep -r "VIRT_RWX_STORAGE_REQUIRED" src/` — returns matches in validation.ts and validation.test.ts
5. `grep -r "rwxStorageRequired" src/i18n/` — returns matches in all 4 locale files
6. `grep -r "rwxStorageAvailable" src/engine/types.ts src/engine/defaults.ts src/composables/useUrlState.ts src/components/wizard/Step2WorkloadForm.vue` — returns matches in all 4 files
</verification>

<success_criteria>
- WARN-04 satisfied: validation guard checks both odfEnabled and rwxStorageAvailable, so non-ODF RWX storage suppresses the warning
- WARN-05 satisfied: old code VIRT_RWX_REQUIRES_ODF absent from entire src/ tree; new code VIRT_RWX_STORAGE_REQUIRED used in validation, tests, and locales
- All 5 Phase 14 success criteria from ROADMAP.md met
- Full test suite green with zero regressions
- TypeScript strict mode compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/14-warning-fix/14-01-SUMMARY.md`
</output>

name: Security
on:
  push: { branches: [gsd/v1.0-milestone] }
  pull_request: { branches: [gsd/v1.0-milestone] }
  schedule:
    - cron: "23 4 * * 1"
permissions:
  contents: read
  security-events: write
  actions: read
jobs:
  security:
    uses: fjacquet/ci/.github/workflows/web-security.yml@v1

name: CI
on:
  push: { branches: [gsd/v1.0-milestone] }
  pull_request: { branches: [gsd/v1.0-milestone] }
permissions:
  contents: read
jobs:
  ci:
    uses: fjacquet/ci/.github/workflows/web-ci.yml@v1
    with: { node-version: "24" }
